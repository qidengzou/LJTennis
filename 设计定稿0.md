# LJTennis 设计定稿 0

> 微信小程序 · 业余网球单双打赛事：**报名 → 记分 → 出分**
>
> 本文只写**结论**。推导过程、被推翻的方案、发现问题的经过在
> `设计流程.md` —— 那是过程日志，**结论以本文为准**。

---

## 1 · 定义与边界

**LJTennis 让业余网球赛事的「报名 → 记分 → 出分」在一个微信小程序里闭环完成。**

第一版做**双打 + 单打**：男双 MD / 女双 WD / 混双 XD / 男单 MS / 女单 WS。

> 单打是靠 `Entry`（1~2 人）抽象免费得到的 —— 加它没有改模型，
> 校验、发球轮转、签表、积分全部沿用。

### 明确不做

- 自由约球（C2C 撮合）
- 全循环、瑞士制等赛制
- **算出来的水平**（`systemLevel`）—— 字段留位，算法不做
- 水平上榜
- 俱乐部审批 —— 免审核直接加入
- 直播比分面板（OBS）—— **延后不是否决**，见 §9
- 视频、社区内容、场地预订

---

## 2 · 角色与权限

| 角色 | 在哪 | 能做什么 |
|---|---|---|
| 选手 | 小程序 | 报名、看签表、记分、确认比分、看积分和水平 |
| 俱乐部管理员 | 小程序（现场）+ 网页（赛前） | 现场：看报名名单、记分、改比分、处理争议<br>赛前：建俱乐部、建赛事、抽签、排赛程、导入 |

**只有两个角色。** 一个人可以同时是两者 —— 现实里几乎一定是。
不做身份切换，管理员只是在「我的」页多一块入口。

### 端的分界线：现场 vs 赛前

| 端 | 管什么 | 为什么 |
|---|---|---|
| 小程序 | 看报名名单、记分、改比分、处理争议 | 人在球场，只有手机 |
| 网页后台 | 建俱乐部、建赛事、抽签、排程、批量导入 | 拖签表和批量操作小屏做不了 |

分界线是**「现场 vs 赛前」**，不是「简单 vs 复杂」。

> 连带结论：**组织者身份从网页端开始**。小程序只承接后续 ——
> 所以「我管理的」没有俱乐部时是空态加一句指路，不给按钮。

**网页后台第一版就要有。** 8 屏已出稿（`design/admin/`）：登录 · 选俱乐部 ·
建赛 · 报名列表 · 配对 · 签表 · 排程 · 首页。**创建俱乐部只在网页端**，
小程序不给入口。

---

## 3 · 数据模型

```
俱乐部 Club
 └─ 赛事 Tournament        （带 A/B/C 等级）
     └─ 比赛 Event          （男双 MD / 女双 WD / 混双 XD / 男单 MS / 女单 WS）
         └─ 参赛主体 Entry  = 1~2 名选手
             └─ 场次 Match
```

### 一个赛事可以挂多个比赛

男双、女双、混双同时办是常态，**不拆成三个赛事** —— 它们共享场地、日期、
俱乐部和一张封面。选手报名时**挑一个**自己要打的。

`Entry`、签表、积分都挂在 `Event` 上，所以各比赛之间完全独立：
男双满员不影响女双，男双的签表也不会跟混双混在一起。

### 系统只管收报名

**不做聪明的自动处理。** 名额、组队、异常一律走人：

| 情形 | 谁来处理 |
|---|---|
| 组队 | 主办方在后台配 |
| 配不上、退赛、临时缺人 | 主办方决定 —— 该退款退款，该参加参加 |
| 超时 | 只做自动作废与退款这两件保底动作 |

> 这条解释了今天很多决定为什么长这样：手动配对、代为指定搭档、
> 免付开关、拆开已配的组。**系统负责把钱和名额记清楚，判断留给人。**

### 术语表 · 界面上必须这么叫

| 模型 | 界面用词 | 例子 | 绝不能叫 |
|---|---|---|---|
| `Tournament` | **赛事** | 春季双打赛 | — |
| `Event` | **比赛** | 男双 · 女双 · 混双 | ~~项目~~ |
| `Match` | **场次** | 场次 12 | ~~比赛~~ |
| `Entry` | **报名** / 参赛 | 张伟 / 李强 | ~~组合~~（单打只有一人） |

> 「比赛」和「场次」曾经反着用 —— `Event` 叫项目、`Match` 叫比赛。
> 「项目」太术语，用户不说这个词；换成「比赛」后必须把 `Match` 让出来，
> 否则同一个词指两样东西：报名页说「报名这个比赛」，记分页说「这场比赛」。
>
> **「场次」不是新造的词** —— 签表和排程本来就在用（「场次 12」）。

**`Entry` 是关键抽象**：单打时 1 人，双打时 2 人。签表、积分、记分全部挂在
Entry 上，单双打共用一套结构。

**俱乐部**解决了「谁能办赛」这个悬空问题：你是某俱乐部管理员，所以能以
俱乐部名义办赛。平台不必维护全局组织者白名单。

### 选手与俱乐部

| | 规则 |
|---|---|
| 报名 | **完全开放** —— 谁的赛都能报，不要求先入会。但**必须先注册**：没有占位账号、没有代建账号，任何人参赛前都要自己走一遍注册 |
| 加入 | 可选，免审核直接加入；管理员可移除成员 |

> 封闭制（必须入会才能报名）会挡住分享拉新 —— 朋友转发的报名链接，
> 对方要先申请入会，转化断在那一步。俱乐部对选手是**社区层**，不是门禁。

---

## 4 · 页面清单 · 26 页

### tabBar 三项：赛事 / 积分 / 我的

`tabBar` 是 `app.json` 静态配置，所有人看到的一样。
**不给组织者开 tab** —— 按身份显示不同 tab 只能上自定义 tabBar，
为 1% 的组织者让 99% 的人多看一个 tab 不划算。组织者入口在「我的」页里。

### 主包 17 页

三个 tab（`tournament` `rank` `me`）· `auth/register` ·
`tournament/detail` · `event/detail` · `club/detail` ·
`entry/create` · `entry/partner` · `entry/invite` ·
`draw` · `draw/overview` ·
`score/setup` · `score/live` · `score/confirm` ·
`org/match` · `me/entries`

### 分包 9 页

`me/records` · `me/level` · `me/profile` · `me/settings` ·
`player/detail` · `rules/points` ·
`org/index` · `org/tournament` · `org/entries`

| 页面 | 干什么 |
|---|---|
| `entry/create` | **报名确认** —— 费用、须知、协议 → 唤起微信支付 |
| `entry/partner` | **邀请队友** —— 常搭档 / 搜索 / 直接转发，生成组队链接 |
| `entry/invite` | **组队邀请落地页** —— 被邀请的人看的，免登录 |

> 原来的 `entry/pay` 删掉：付款用 `wx.requestPayment` 在当前页唤起，
> 不需要独立页面。候补转正后的付款从「我的报名」直接唤起。
>
> 原来的 `entry/create`（发起报名选搭档）拆成两页 ——
> 报名时不再选人，选人是报名之后的独立动作。

### 进主包的判据

只要占**一条**就进：

1. **分享落地页** —— `tournament/detail` `event/detail` `club/detail`
2. **球场里要用** —— `draw` `score/*` `org/match`（信号差，分包下不下来）
3. **高频** —— 三个 tab、`me/entries`

> `org/match` 进主包的理由和记分器完全一样：**组织者也站在球场边**。

### 未注册时的 tab 策略

| tab | 未注册 | 理由 |
|---|---|---|
| 赛事 | 不拦，完整可浏览 | 获客前提，分享落地页要能看 |
| 积分 | 不拦 | 榜单是公开信息 |
| 我的 | **拦**，显示未注册态页面 | 整屏都是个人数据 |

「我的」用**未注册态页面**而不是硬跳转 —— tab 页硬跳注册会把人困住退不
回去，还会打乱 tabBar 状态。

> **赛事与积分两个 tab 压根不区分注册状态。** 赛事首页只关心「我的报名
> 列表空不空」，未注册的人和已注册但没报过名的人看到的完全一样 ——
> 一个状态覆盖两种人，不需要为「访客」单独做一套。
>
> 判据：**拦不拦看内容属谁，不看用户是谁。**

注册页要携带**来源与回跳目标**。跨 tab 时走本地存储 ——
`wx.switchTab` 带不了参数。

---

## 5 · 四条关键路径

**P1 · 报名**
赛事卡「报名」→ 赛事详情（**选比赛**）→ 比赛详情 → 报名确认 → 付款 →
「已报名」→ 邀请队友 / 等主办方配对 → 已确认 → 赛前提醒（订阅消息）

> **赛事卡上的「报名」是导航不是动作** —— 点了进赛事详情，那里列着这场赛事
> 开了哪几个比赛、各自还剩多少名额，挑一个再进比赛详情看费用和须知。
> 不直接跳付款。

**P2 · 比赛日**（最高频、条件最苛刻）
打开小程序 → 赛事 tab 今日赛程区 → 进入记分 → 每分 1 tap → 结束 → 对方确认

> 记分收在赛事 tab 首页，比赛日是 **2 tap**。今日赛程区**必须永远置顶** ——
> 这一区没做好会退化成 3–4 tap，球场边会直接放弃记分。**第一版最需要盯的一处。**

**P3 · 赛后**
积分变化 → 水平变化 → 榜单位置 → 下一个赛事推荐

**P4 · 组织者现场**
我的 → 我管理的 → 选赛事 → 看到异常场次 → 处理

> P4 的物理条件与 P2 相同（球场、信号差、单手），**套用同一套设计标准**：
> 大点击区、极简、可离线看。

---

## 6 · 报名状态机

**报名就是付钱进来。组队是之后的事。**

```
双打   报名（付自己那半）→ 待组队 ──┬─ 主办方配对 → 已确认
                                    └─ 报名截止仍未组队 → 作废，退款

单打   报名（付款）→ 已确认

满员   加入候补（不付）→ 转正 → 24h 内付 → 待组队 / 已确认

已确认 → 申请退款 → 退款中 → 已退款
```

> **双打只有一条路径。** 接受邀请、扫码报名、朋友推荐进来的，走的是同一条 ——
> 区别只是 `invitedBy` / `referredBy` 这两个来源字段填没填。组队一律由主办方在后台完成。

付完钱回到赛事页就显示**「已报名」**，然后才出现「邀请队友」「邀请朋友」两个按钮。

`EntryStatus`：`seeking_partner` · `waitlisted` · `promoted` ·
`confirmed` · `cancelled` · `refunding` · `refunded`

> **没有 `pending_partner`。** 发出邀请的人和没发的人处在**同一个状态** ——
> 都是待组队，区别只是有没有一条链接在外面。那是 `Entry` 上的一个附加字段
> （`inviteActive` / `invitedAt`），不是一种状态。
>
> 这是本轮消掉的第二个状态（第一个是 `pending_payment`）。

### 每人付自己的

**没有代付。** 发起人不替搭档垫钱 —— 两个陌生人之间那样很尴尬，
单独报名时更是根本没有「发起人」这个角色。

**付款发生在报名那一刻**，不是组合成立后。所以两人碰到一起时都已付过，
`pending_payment` 这个状态和它的 24 小时计时器一并消失。

代价是报名截止仍未组队要退款。退款流程本来就有，换来的是少一个状态、
少一个计时器。

### 两个等待态，每个都必须有自动出口

| 等待态 | 超时后 |
|---|---|
| ① 待组队 | **报名截止时**作废并退款 |
| ② 候补转正 24h | **自动顺延下一位候补** |

时限由平台固定，组织者不可配。建赛表单里只有「报名截止」是每场都不同的。

> **②「候补转正 24h」是后端定时任务。** 不做的话每个空位都会烂在一个
> 不响应的人手里，而业余赛退赛率很高。

### 什么状态算占名额

**付了钱的占名额。** 即 `seeking_partner` · `promoted` · `confirmed`；
只有 `waitlisted` 不占（候补不预付）。

> 连带推出：报名时若名额已满，**直接进候补且不收款** ——
> 不会出现「付了钱却排在候补队列里」。

> ⚠️ **代码与此不符**：`handlers/entry.js` 判名额时只数 `confirmed`
> （`tx.count(C.ENTRIES, { status: 'confirmed' })`），于是 16 组的比赛已确认 15 组时，
> 多对同时接受邀请会**全部**通过 —— 超发。修法是把计数条件改成上面三个状态。
> 另有 19 处代码仍引用已废弃的 `pending_payment`。**待修。**

### 免费赛事

报名费为 0 时整段跳过支付 —— 组合成立直接进「已确认」。
否则第一场社区友谊赛会卡在一个支付 ¥0 的页面上。

### 退款规则

| 情形 | 处理 |
|---|---|
| 赛前 7 天以上 | 全额退 |
| 赛前 3–7 天 | 退 50% |
| 赛前 3 天内 | 不退 |
| 组织者取消赛事 | 全额退 |
| 换搭档 | 不涉及退款 |

## 7 · 组队

**所有组队都是主办方手动做的。** 报名只产生一条单人 `Entry`，
邀请只留下一条线索。

已报名的人有两个按钮：

| | 邀请队友 | 邀请朋友 |
|---|---|---|
| 意思 | **跟我一队** | **也来打这个比赛** |
| 落地页 | 组队邀请页 | 赛事详情页 |
| 对方报名后 | 记 `invitedBy = 我` | 记 `referredBy = 我` |
| 参与配对 | **是** —— 建议配对靠它 | **否** —— 只做来源统计 |

> **两个字段必须分开，这是正确性问题不是记账问题。**
> 共用一个的话，配对屏会把「张伟叫李强来打比赛」误读成「张伟想跟李强一队」，
> 生成一条**看起来很有道理的错误建议** —— 管理员多半就点确认了。
>
> `referredBy` 第一版**不做界面**，只把字段存下。冷启动阶段「人从哪来」
> 这类数据丢了就补不回来，而多存一个字段几乎不要钱。

**什么都不点也没关系** —— 待组队是默认状态，主办方会配。

### 接受邀请只记一个字段

点「接受」的人走的是**普通报名流程**：注册（若未注册）→ 付自己那一半 →
落进待组队池，**外加一条 `invitedBy`**。

从「邀请朋友」的分享链接进来的人，走的是完全相同的流程，
只是记的字段变成 `referredBy` —— 它不影响配对。

**不自动组队。** 两人仍是两条独立的单人 `Entry`，等主办方把它们并成一条。

> 这样做的好处是**只有一条报名路径**。接受邀请、扫码报名、朋友推荐进来的，
> 在数据上完全一样，区别只是来源字段填没填。
> 没有合并时序、没有并发抢位、没有第二套状态。

### 组队邀请链接

**可群发，先到先得，接受即失效** —— 避免两个人都以为自己是我的搭档。

生成链接前三个入口，产出同一种链接：常搭档列表（排最上面，唯一不需要
输入就能选到人的入口）· 搜索昵称/手机号 · 直接转发。
**不做绑定给某个人的排他邀请** —— 与先到先得矛盾。

链接是**免登录的分享落地页**，未注册也看得到。点「接受」才撞登录墙 ——
弹注册页，注册完自动回来继续。这是业余赛拉新最主要的入口。

### 主办方配对

后台「待配对」屏分两块：

- **建议配对** —— 有 `invitedBy` 关系的两人，直接成对列出，**一键确认**
- **待配对** —— 其余的人，勾两个 → 配成一组

> 建议配对是「机器铺底，人处理例外」在这一屏的落法：常见情形一键完成，
> 剩下的手动。与抽签、排程同一条思路。

两条配对辅助：

- **性别校验反过来用** —— 报男双只列男性候选，混双只列异性候选，配不错
- **按自评水平排序** —— 相近的挨在一起。这是注册时那个滑杆第一个真正
  被用到的地方

配成一组时若已满员，照样进候补，走同一条 `acceptResult`。
报名截止仍未组队 → 作废退款。

## 8 · 记分

### 赛制

只做两种，覆盖约 90% 的业余赛事：

- **单败淘汰** → 树状签表
- **小组循环取前二出线** → 小组积分表 + 树状签表

### 比赛格式

| key | 说明 |
|---|---|
| `short6_gp` | **6 局金球**、6-6 抢七、三盘两胜 —— 默认 |
| `short6_tb` | 6 局短盘（占先制）、6-6 抢七、三盘两胜 |
| `single6_tb` | 单盘 6 局、抢七 |
| `tb10` | 抢十 |

由赛事设定，选手不可改（避免两边记的赛制不一致）。

**金球（no-ad）**：平分后不打占先，**一分定胜负**。业余赛的通行做法，
省时间。与「短盘」是两个维度 —— 短盘讲一盘打几局，金球讲平分怎么办。

> ⚠️ **引擎不支持**：`tennis.js` 的判定是 `points >= 4 && 差 >= 2`，
> 那是占先制。金球要在 `FORMATS` 加 `noAd: true`，判定改成
> `points >= 4 && (cfg.noAd || 差 >= 2)`；`pointLabel` 也不该再出现「占先」。
> **待修。**

### 权限：一个开关，不是一套配置

- 默认**选手记分**，每场指定一名选手
- **管理员随时也能记**
- **管理员可以对某一场关掉「选手记分」**

> 建赛那天并不知道哪场会直播、哪场会吵起来。等那场要开打了再关，
> 才是真实的决策时机。

**管理员记的分不需要选手确认。** 管理员本来就有「改比分」的权力，让他记的
分再回去等选手批准是矛盾的；真出争议时，让吵架的一方去确认会直接卡死。

### 确认流程

```
选手记分 → 结束自动生成确认卡 → 对方组合任一人 1 tap 确认 → 比分生效
                                      ↓ 有争议
                                 冻结，上报管理员裁定
```

`MatchStatus`：`pending` · `live` · `pending_confirm` · `confirmed` ·
`disputed` · `walkover`

未确认的比分在签表和战绩里要有可见的状态标记。

### 双打发球轮转

记录到**「谁在发球」**的粒度 —— 这是双打里最有价值的个人数据（能算保发率）。

- **双打**开局设定一次：A1 → B1 → A2 → B2（2 tap），之后每局自动轮转
- **单打**是两人轮流，每局换发，无需设定
- 用户全程零操作
- 抢七内第 1 分后换发，之后每 2 分换一次（ITF 规则）

### 存储粒度

`MatchScore` 只存 **`sets` + `serveOrder`**，**不存逐分数据**。

> 这个取舍有两次回报：记分引擎能写成纯函数；
> 「改比分」只是编辑最多三盘局分，是个小屏不是重屏。

**记分全程离线可用**，签表进主包 —— 球场信号差。

---

## 9 · 积分

### 组织者选等级，平台定分值

| 等级 | 定位 |
|---|---|
| A | 市级以上公开赛 |
| B | 俱乐部公开赛 |
| C | 内部友谊赛 |

各等级的轮次分值由平台固定。**具体数字待定，不阻塞画页面。**

> 为什么不让组织者自由填分值：**全局榜会失去可比性**。赛事 A 冠军给 1000、
> 赛事 B 给 100，榜首可能只是参加了分值虚高的赛事。ATP 榜单成立恰恰因为
> 分值全局统一 —— 1000/500/250 是等级，不是自由输入框。

### 榜单

- 排**人**不排组合 —— 搭档会换，排组合没有连续性
- 默认**全局**（「全城第几」是更强的动机），可按俱乐部筛选
- 筛选维度：**类型 / 赛季 / 俱乐部**

**「按俱乐部筛选」筛的是赛事，不是人。** 只统计在该俱乐部办的赛事里拿到的
积分 —— 选手可加入 0 个或多个俱乐部，归属是社区层，与积分无关。
**榜单行不显示俱乐部。**

榜单行：名次 · 昵称 · 积分 · **参赛场次**。
场次必须显示，否则「打得多分就高」会让榜单显得不公。

---

## 10 · 三个数字

| | 积分 | 自评水平 | 系统水平 |
|---|---|---|---|
| 字段 | — | `selfRatedLevel` | `systemLevel` |
| 怎么来 | 参赛累加 | 注册时拖滑杆 | 从比分推算 |
| 用户能改 | ✗ | ✓ 随时 | ✗ |
| 会不会降 | 不降（赛季清零） | — | 会升会降 |
| 第一版 | ✅ 上榜 | ✅ | ⬜ 字段留位，算法延后 |

自评用 **NTRP 2.0–5.0**，0.5 一档（1.5 实际没人自评到，不放出来）。
只做赛事初始分组，不依赖算法。

界面上并排显示，标签写全「自评」「系统」。
**系统值没算出来时显示「—」，绝不用自评顶替** ——
否则用户打输三场发现数字没动，会认为系统坏了。

---

## 11 · 注册

字段：**昵称 · 手机号 · 性别 · 水平**

| 字段 | 交互 |
|---|---|
| 昵称 | `<input type="nickname">` —— 点输入框，微信在键盘上方推候选。**不弹授权、不收费、个人主体可用**，是 `getUserProfile` 废弃后唯一的路子 |
| 手机号 | **只能微信授权**（`getPhoneNumber`），已去掉手动输入 |
| 性别 | 分段，默认男 |
| 水平 | 刻度滑杆，默认 2.5 |

**触发点两个**：点「报名 / 接受邀请」时，或点「我的」tab 时。

### 底图：浅蓝 + 赞助商条

注册页是**唯一一张有品牌底图的页**，形式是「纯色天空 + 底部赞助商条」，
**不是**一张铺满的照片。

| | |
|---|---|
| 页面底色 | `--c-sky: #98C5FE` |
| 赞助商条 | `images/register-sponsors.jpg` · 750 × 330 · 54 KB |
| 母版 | `design/assets/register-bg-source-lt.jpg`（750 × 1600） |

母版上半 55% 是空白浅蓝，九宫格 logo 在 **55%~72%**、品牌字在 **76%~84%**。
表单卡（四字段 + 主按钮）本身就要 1100rpx 上下，**整张铺满时卡片正好落在
logo 上**，赞助商全被盖住。所以拆开用：

- 上半的浅蓝 → 页面纯色底（任何屏幕比例都不会裁）
- 中间的九宫格 logo → 裁成 750×330 一条，**当内容排在表单后面**，跟着滚
- 底下两行品牌字 → **不要**，连它一起裁整条会到 520rpx，压在表单下面太长

页面上也没有 LJTENNIS 小字和「你正在报名什么」那行上下文 ——
标题只留**「上场之前」**一行。

`--c-sky` 从母版逐位取色，和图里的蓝必须完全一致，否则接缝会露出来。
**换底图就重新取色。**

> **待办：母版只有 750px 宽（@1x）。** 赞助商条 1:1 铺满 750rpx，@3x 上
> logo 会发虚，`npm run check` 会一直警告到换成 2250px 宽的原图为止。

### 手机号的实现约束

- 客户端只拿得到 `code`，**真号必须在云函数换**
- `code` **有效期 5 分钟且一次性** —— 所以**授权当场就换**，
  不能攥着等用户填完表提交
- 换出来的真号存服务端，**只把掩码回给前端**
- `register` 只认服务端暂存，不接受客户端传号码充当「已验证」——
  否则谁都能凭空认领任意号码

---

## 12 · 设计系统 · 令牌 v0.4

完整令牌：`mp/miniprogram/styles/tokens.wxss`

### 颜色

主色 **硬地场蓝 `#2E4FD8`**。语义色红绿橙（赢/输/待处理）。

**单一用途的约束色**（别处禁用）：

| 色 | 唯一用途 |
|---|---|
| 网球黄 `--c-serve` | 发球方指示点（双打=四人中的当前发球者） |
| 微信绿 `--c-wx` | 微信原生能力入口（支付、手机号授权） |

**头像色板刻意是一组冷色**，不含绿/红/橙 ——
一个绿色头像会被误读成「这个人赢了」。

报名状态 → 颜色是**固定映射**，不可临时改：
待组队 / 候补 / 候补转正 = 橙，已确认 = 绿，已退款 / 作废 = 灰。

**全站不做深色模式。** `app.json` 的 `darkmode` 为 `false`，样式里也没有
`prefers-color-scheme` 分支 —— 注册页的浅蓝底和赞助商底图都是固定值，
翻色会让浅色卡片嵌深色控件，底图也跟不着色。`npm run check` 守着这条。

### 三个组件配方

**赛事等级 A/B/C** —— 用主色三档强度，**不新开色相**：

| 等级 | 样式 |
|---|---|
| A | 实心 `--c-brand` + 白字 |
| B | `--c-brand-weak` 底 + `--c-brand` 字 |
| C | 透明 + `--c-line` 描边 + `--c-ink-3` 字 |

> 色轮已无空位（绿=赢+微信、红=输/危险、橙=进行中、黄=发球、紫青=头像）。
> 更要紧的是 **A/B/C 是有序不是并列** —— 三个色相只能表达「三种」，
> 表达不了「三档」。

**待办角标** —— 复用 `--c-live` 橙（语义就是「有事待处理」），不新增颜色。

**空值「—」** —— `--c-ink-3`，**字号与真值一致**。两条规矩：
必须与加载态形态不同（空值是实心字符，加载是骨架条）；
字号不能缩，否则真值出来那一刻整块会跳。

### 控件高度

```
--h-control: 88rpx   /* 输入框、分段、副按钮 —— 44pt，iOS 最小点击区 */
--h-cta:     96rpx   /* 主按钮 */
```

### 八条排版规则

1. **双打组合名**：宽容器一行 `A / B`，窄容器两行堆叠，**绝不只截断其中一个**
   （单打只有一个名字，不适用）。
   375pt 设备上名字列可用 244pt，超过就堆叠 —— 中文姓名永远触发不了，
   只有长英文名会
2. **抢七比分用上标** `7⁷ 6⁵`，不用 `7-6 (7-5)`
3. **发球指示是网球黄的唯一用途**
4. **水平可信度 < 60% 显示为区间** `6.8–7.6`，用 en dash 不用连字符
5. **满员不是错误状态** —— 用橙不用红，按钮变「加入候补」不灰掉
6. **头像色不能用语义色**
7. **倒计时必须写明超时后果** —— 「23:41」无意义，「超时释放名额 23:41」才有
8. **球场边使用的所有可点元素不低于 88rpx** —— 约束 P2 与 P4 两条路径

---

## 12.5 · 赛事封面

组织者建赛时**可选**上传一张封面，用在赛事卡和赛事详情页顶部。

| | |
|---|---|
| 出图 | **1125 × 630** JPEG（比例 750:420） |
| 大小 | ≤ 120 KB |
| 存放 | **云存储**，不进包 —— 每场赛事一张，包内放不下 |
| 裁切 | `aspectFill`，主体须落在中间 60% |

**没传封面不是错误状态。** 用主色渐变加巨大的赛事名首字水印兜底 ——
放灰色占位图会让那场赛事看起来像坏了。

照片上压一层渐变（顶 .46 → 底 .82），白字和主色按钮才站得住。
`--c-on-photo` 是照片上文字的唯一取值。

> 注册页**不再**用这套 —— 它换成了浅蓝纯色底，遮罩已拆掉（见 §11）。
> 封面是这一套遮罩现在唯一的使用者。

> 等级标签 `tier.c` 在照片上有**唯一变体**：描边从 `--c-line` 改成半透明白 ——
> 浅灰描边压在深色遮罩上看不见。

---

## 13 · 硬性前提（上线门槛）

这三条**写代码解决不了**，且都以「周」计，要提前并行启动：

| 前提 | 卡住什么 |
|---|---|
| **企业主体** | 微信支付 **和** 手机号快速验证，两者都要 |
| **用户隐私保护指引过审** | 未过审 `getPhoneNumber` 直接不可用，报 `api scope is not declared`（不是权限错误，容易误判）。后台路径：设置 → 基本设置 → 服务内容声明 → 用户隐私保护指引 → 勾选「手机号」 |
| **商户号** | 报名费支付。没有企业主体办不了商户号 |

另：手机号快速验证组件 **2023-08-26 起按次收费**，每次成功约 0.026–0.03 元。

---

## 14 · 延后但要留接口的

**直播比分面板（OBS）。** 它要逐分实时，与「记分离线优先、只存每盘比分」
直接冲突。

出口是：**直播场次天然有网**（没网也直播不了），可走在线分支。
记分权限已经定宽（管理员随时能记 + 单场开关），以后加只是多一个消费端，
**不必改模型**。

OBS 的 browser source 加载的是一个 URL —— 那必然属于网页端。

---

## 附 · 交付物索引

| | 文件 |
|---|---|
| 实体与状态机（过 `tsc --strict`） | `api/types.ts` |
| 接口清单 · 鉴权 · 幂等 · 定时任务 | `api/endpoints.md` |
| 云开发对契约的影响 | `api/cloudbase-notes.md` |
| 设计令牌 v0.4 | `mp/miniprogram/styles/tokens.wxss` |
| 图片规格 | `design/image-spec.md` |
| **小程序骨架 · 26 页结构图** | `design/skeleton/` |
| **赛事首页 · 线框与高保真** | `design/home/` |
| 阶段 2 线框（第二版 · 7 屏） | `design/stage2/` |
| 组织者网页后台（8 屏） | `design/admin/` |
| **注册页（2 屏）** | `design/register/` |
| **报名确认与付款后（2 屏）** | `design/entry/` |
| 早期高保真稿（**已过时，只作参考**） | `design/hifi-*.html` |
| 过程日志（**结论以本文为准**） | `设计流程.md` |

> 画布按**一屏一张**组织 —— 结构图只放结构图，单屏的线框与高保真各自独立成张。
>
> `design/hifi-*.html` 是阶段 1–2 的产物，里面还留着「项目」「待支付」
> 「待搭档确认」「发起人一人付清」这些**已经废掉的说法**。要看现在的模型，
> 只看本文和 `design/*/` 下的画布。
