# LJTennis 产品需求文档

> 微信小程序 · 业余网球单双打赛事：**报名 → 记分 → 出分**

**这份回答「做什么、给谁、什么算做完」，不回答「怎么实现」。**
表怎么建、组件怎么拆、用不用云开发 —— 都不在这里。

| 要找什么 | 看哪份 |
|---|---|
| 做什么、为什么、什么算做完 | **`PRD.md`**（本文） |
| 数据模型、页面、枚举、实现约束 | `SPEC.md` |
| 颜色、字号、控件高度、图片规格 | `design/DESIGN.md` |
| 实体与状态机的**真身** | `api/types.ts`（过 `tsc --strict`） |
| 接口、鉴权、幂等、定时任务 | `api/endpoints.md` |

> **一个事实只定义一处，别处只引用。** 冲突时的优先级：
> 类型和状态以 `api/types.ts` 为准，接口以 `api/endpoints.md` 为准，
> 产品规则以 `PRD.md` 为准，页面结构以 `SPEC.md` 为准，
> 视觉以 `mp/miniprogram/styles/tokens.wxss` 为准。

---

## 0 · 目标与成功指标

### 要证明什么

**一场业余网球赛能全程在小程序里跑完，主办方不再需要微信群 + Excel + 手写签表。**

第一版不追求规模，只追求**闭环**：一场 16 队的男双，从开报名到出积分，
中途不掉回微信群，就算成了。

对应到三段现状：

| 今天怎么做的 | 第一版要替掉的 |
|---|---|
| 群接龙报名 + 转账截图对账 | 报名即付款，名额和钱由系统记 |
| 手写纸记分，赛后再录一遍 | 球场边 1 tap 一分，结束即出分 |
| Excel 算积分，通常隔天 | 比分确认后自动累加 |

### 成功指标

> 🚧 **数字是我按规格里的判断提的，你按实际情况改。**
> 定义写死了，改的是右边那一列。

| 指标 | 怎么算 | 提议值 |
|---|---|---|
| **记分覆盖率** | 已结束场次里，有比分且已确认的比例 | 首场 ≥ 70%，第三场 ≥ 90% |
| **进入记分的成本** | 打开小程序到记下第一分 | ≤ 2 tap，实测 ≤ 15 秒 |
| **报名完成率** | 进报名确认页 → 付款成功 | ≥ 80% |
| **成组成功率** | 报名截止时因没进签位而作废的比例 | ≤ 10% |
| **赛后出分耗时** | 最后一场结束 → 积分可见 | ≤ 10 分钟 |
| **俱乐部复用率** | 办过一次的俱乐部再办第二次 | ≥ 60% |

前两条是命门。§3 的 P2 说得很直白：**这一区没做好会退化成 3–4 tap，
球场边会直接放弃记分** —— 那样后面五条指标全部失去意义。

最后一条是唯一能说明「真的有用」的：办第一次可能是人情，办第二次才是价值。

### 失败信号

不是指标没达标，是**方向错了**，看到就要停下来重想：

| 信号 | 说明什么 |
|---|---|
| 球场边改回手写纸，赛后补录 | P2 输了。记分是整个产品的地基，它塌了积分和榜单都是空的 |
| 主办方仍在微信群里收报名 | 报名链路有断口 —— 多半是「先付款再成组」这条规则业余圈接受不了 |
| 选手反复问「我配上搭档没有」 | 待编排这个状态没表达清楚，或者签表排得太慢 |
| 组织者要求自己填积分分值 | §7 那条「平台定分值」判断错了，全局可比性和主办方自主权得重新权衡 |

### 这一节是用来裁决的

**「要不要做 X」的争论，一律拿到这里裁。**
理由是「能提高上面某条指标」才算理由；理由是「别家有」不算。

举个已经裁过的例子：`systemRatedLevel`（算出来的水平）不影响上面任何一条
指标，却要一整套算法和调参 —— 所以留字段不做算法（§11）。

反过来，**直播比分面板一度也在延后里，后来被拉进范围**（§6）。
它同样不直接提升哪条指标，但它是俱乐部愿意办第二次的理由之一 ——
落在「复用率」上。裁决时要问的是**落在哪条指标上**，不是「有没有用」。

---

## 1 · 定义与边界

**LJTennis 让业余网球赛事的「报名 → 记分 → 出分」在一个微信小程序里闭环完成。**

第一版做**双打 + 单打**：男双 MD / 女双 WD / 混双 XD / 男单 MS / 女单 WS。

> 单打是靠 `Entry`（1~2 人）抽象免费得到的 —— 加它没有改模型，
> 校验、发球轮转、签表、积分全部沿用。

### 明确不做

- 自由约球（C2C 撮合）
- 全循环、瑞士制等赛制
- **算出来的水平**（`systemRatedLevel`）—— 字段留位，算法不做
- 水平上榜
- 视频、社区内容、场地预订



---

## 2 · 角色与权限

| 角色 | 在哪 | 能做什么 |
|---|---|---|
| 选手 | 小程序 | 报名、看签表、记分、确认比分、看积分和水平 |
| 俱乐部管理员 | 小程序（现场）+ 网页（赛前） | 现场：看报名名单、记分、改比分、处理争议<br>赛前：建俱乐部、建赛事、抽签、排赛程 |

**只有两个角色。** 一个人可以同时是两者 —— 现实里几乎一定是。
不做身份切换，管理员只是在「我的」页多一块入口。

### 端的分界线：现场 vs 赛前

| 端 | 管什么 | 为什么 |
|---|---|---|
| 小程序 | 看报名名单、记分、改比分、处理争议 | 人在球场，只有手机 |
| 网页后台 | 建俱乐部、建赛事、抽签、排程、批量导入、**改选手性别** | 拖签表和批量操作小屏做不了；性别是资格判据，不能自助改 |

分界线是**「现场 vs 赛前」**，不是「简单 vs 复杂」。

> 连带结论：**组织者身份从网页端开始**。小程序只承接后续 ——
> 所以「我管理的」没有俱乐部时是空态加一句指路，不给按钮。

### 俱乐部与管理员

**俱乐部回答「谁能办赛」** —— 你是某俱乐部的管理员，所以能以俱乐部名义办赛。**只能在网页后台创建。**

### 谁能建俱乐部

**只有平台管理员。** 普通用户不能自己开俱乐部。

这一层是**平台身份**

| | 是什么 | 谁给的 |
|---|---|---|
| **平台身份** | 你在这个产品里是谁 | 没有自助入口。靠人工改库引导 |
| **俱乐部身份** | 你在某一个俱乐部里是谁 | 该俱乐部的创建者指定 |


建俱乐部时**必须同时指定一个创建者**，那个人立刻成为该俱乐部的 owner
（会员关系 `status: active` + `role: owner`）。不允许出现无主俱乐部。


一个俱乐部**可以有多个管理员**，身份分三档：

| 身份 | 能做什么 |
|---|---|
| **创建者** owner | 管理员能做的一切 + **任命/撤销管理员** + 转让俱乐部 |
| **管理员** admin | 建赛、排签表、审批入会、移除普通会员 |
| 会员 member | 归属而已 —— 榜单能按俱乐部筛到、能被主办方挑来补人 |

五条防死锁的规则，缺一条就会出事：

1. **管理员首先得是会员** —— 身份挂在会员关系上，「不是会员却能管俱乐部」
   不成立。移除会员时身份必须一并降回普通会员
2. **只有创建者能任命管理员。** 管理员之间不能互相任命 ——
   否则创建者会在自己俱乐部里失去控制
3. **管理员不能移除创建者，也不能移除其他管理员。** 只能管普通会员 ——
   不然两个管理员吵架时会互相踢，最后没人管得了
4. **创建者唯一，且不能直接退出**，必须先转让。转让是一个原子操作
   （降旧的、升新的，一起改）—— 中间断了就出现无主俱乐部
5. **首个创建者由平台管理员授予** —— 不能由俱乐部身份来判，
   那会死锁：第一个俱乐部谁都建不了

> **身份只有一处真相**：`Membership.role`。不在 `Club` 上另存一份
> `adminIds` 数组 —— 两处存必然不一致，而且「我管哪些俱乐部」那个查询
> 在数组方案里要扫全表。

### 入会与审批

加入俱乐部**要管理员审核**。

| | 规则 |
|---|---|
| 申请 | 任何已注册用户可申请加入任一俱乐部 |
| 审核 | 管理员在网页后台批 / 拒，可批量 |
| 状态 | 申请中 · 已入会 · 已拒绝 · 已移除 |
| 移除 | 管理员随时可移除成员，移除后可再申请 |

> ⚠️ **审批只管入会，绝不管报名。报名必须保持完全开放。**
>
> 这是这一节唯一的红线。封闭制（必须入会才能报名）会挡死分享拉新 ——
> 朋友转发的报名链接，对方要先申请、等审核，转化就断在那一步。
> 俱乐部对选手是**社区层，不是门禁**。
>
> 换句话说：不入会照样能报名打球；入会拿到的是**归属**（榜单按俱乐部
> 筛选、俱乐部页里露出），不是**资格**。

**必须先注册**：没有占位账号、没有代建账号，任何人参赛前都要自己走一遍注册。

**网页后台第一版就要有。** 8 屏已出稿（`design/admin/`）：登录 · 选俱乐部 ·
建赛 · 报名列表 · 配对 · 签表 · 排程 · 首页 —— 其中**配对与签表已合成一屏**
（见 §5），画布尚未重画。**创建俱乐部只在网页端**，
小程序不给入口。

---

---

## 3 · 关键路径

**P1 · 报名**
赛事卡「报名」→ 赛事详情（**选比赛**）→ 比赛详情 → 报名确认 → 付款 →
「已报名」→ 邀请队友 / 等主办方在签表里成组 → 正选 → 赛前提醒（订阅消息）

> **赛事卡上的「报名」是导航不是动作** —— 点了进赛事详情，那里列着这场赛事
> 开了哪几个比赛、各自还剩多少名额，挑一个再进比赛详情看费用和须知。
> 不直接跳付款。

**P2 · 比赛日**（最高频、条件最苛刻）
打开小程序 → 赛事 tab 今日赛程区 → 进入记分 → 每分 1 tap → 结束 → 对方确认

> 记分收在赛事 tab 首页，比赛日是 **2 tap**。今日赛程区**必须永远置顶** ——
> 这一区没做好会退化成 3–4 tap，球场边会直接放弃记分。**第一版最需要盯的一处。**

**P3 · 赛后**
积分变化 → 水平变化 → 榜单位置 → 下一个赛事推荐

**P4 · 组织者赛前**（网页后台）
建赛 → 报名名单 → **签表（拖人成组 + 排签位，一步完成）** → 排程 → 发布

> **报名之后不是「先配对再抽签」两步，是一步。** 见 §5「签表屏怎么用」。
> 这条路径全在网页端 —— 拖签表和批量操作小屏做不了。

**P5 · 组织者现场**
我的 → 我管理的 → 选赛事 → 看到异常场次 → 处理

> P5 的物理条件与 P2 相同（球场、信号差、单手），**套用同一套设计标准**：
> 大点击区、极简、可离线看。

---

---

## 4 · 报名规则与状态

**报名就是付钱进来。成组是签表上的事。**

```
双打   报名（付自己那半）→ 待编排 ──┬─ 主办方拖进同一签位 → 正选
                                    └─ 报名截止仍未进签位 → 作废，退款

单打   报名（付款）→ 正选

满员   加入候补（**照付**）→ 有人退出 → 立刻转正 → 待编排 / 正选
                              └─ 报名截止仍没转上 → 全额退款

正选 → 申请退款 → 退款中 → 已退款
```

> **交了钱之后只有两种身份：正选 或 候补。没有「已确认」这个状态。**
> 正选占正式名额，候补不占 —— 但候补同样先付了钱，不是「等着付」。
>
> **「待编排」不是第三种身份**，是正选身上的一个待办：双打还差一个人。
> 到报名截止还配不上就作废全额退款。所以名额算的是**全部正选**
> （已成组的 + 还在待编排的），只数已成组的会超发 —— `utils/entry.js` 里
> 那个 `occupiedCount` 就是为这件事存在的。
>
> 界面上一律不出现「已确认」这个词，它在这个产品里只指**比分**已确认。

> **双打只有一条路径。** 接受邀请、扫码报名、朋友推荐进来的，走的是同一条 ——
> 区别只是 `invitedBy` / `referredBy` 这两个来源字段填没填。成组一律由主办方在签表上完成。

付完钱回到赛事页就显示**「已报名」**，然后才出现「邀请队友」「邀请朋友」两个按钮。

> **没有 `pending_partner`。** 发出邀请的人和没发的人处在**同一个状态** ——
> 都是待编排，区别只是有没有一条链接在外面。那是 `Entry` 上的一个附加字段
> （`inviteActive` / `invitedAt`），不是一种状态。
>
> 这是本轮消掉的第二个状态（第一个是 `pending_payment`）。

### 每人付自己的

**没有代付。** 发起人不替搭档垫钱 —— 两个陌生人之间那样很尴尬，
单独报名时更是根本没有「发起人」这个角色。

**付款发生在报名那一刻**，不是组合成立后。所以两人碰到一起时都已付过，
`pending_payment` 这个状态和它的 24 小时计时器一并消失。

代价是报名截止仍未进签位要退款。退款流程本来就有，换来的是少一个状态、
少一个计时器。

### 候补也先收款

**加候补要付钱，跟正式报名一模一样。** 有人退出就立刻转正，
报名截止还没转上就全额退款。

这是本轮最划算的一次交换：

| | 候补不预付（旧） | **候补也先收款（现在）** |
|---|---|---|
| 转正 | 通知本人 → 等他 24h 内付 | **立刻生效**，不用等谁响应 |
| 空位 | 会烂在一个不响应的人手里 | 不会 |
| 状态 | 多一个 `promoted`（转正待付） | **删掉** |
| 计时器 | 候补转正 24h | **一个都不剩** |

> 「候补转正 24h」那个定时任务当初就是为了解决「空位烂在不响应的人手里」。
> **先收款把这个问题从根上消掉了** —— 钱已经在，转正就是改一个状态。
> 定时任务、催付、顺延逻辑一起没了。

代价有两条，都要认：

1. **退款有成本。** 微信支付的手续费退款时不退还，候补退得多是实打实的损失。
   业余赛的候补量不大，这笔钱换掉一整套超时逻辑是划算的
2. **「付了钱也可能打不上」必须写死在须知里**，报名前就说清楚 ——
   不写，第一次退款就会变成客诉

### 两个等待态，同一个出口

| 等待态 | 出口 |
|---|---|
| ① 待编排 | **报名截止时**作废并全额退款 |
| ② 候补 | **报名截止时**仍未转正 → 全额退款 |

**两条都在报名截止那一刻结算**，所以只需要一个定时任务，
而且它跑的时间是确定的 —— 不是一堆各自计时的倒计时。

**时限**由平台固定，组织者不可配。建赛表单里能填的只有两个跟场次相关的：
**报名截止**和**每组队数**（选小组循环时才出现，见 §5）。其余一律平台定。

### 什么状态算占名额

**占正式名额的是 `seeking_partner` 和 `confirmed`。**
`waitlisted` 也付了钱，但它占的是**候补位**，不占正式名额。

> 「付钱」和「占名额」现在是两件事，别再用「付没付」去判名额。
> 判据只有一条：**状态是不是 `seeking_partner` / `confirmed`。**

### 免费赛事

报名费为 0 时整段跳过支付 —— 组合成立直接进「正选」。
否则第一场社区友谊赛会卡在一个支付 ¥0 的页面上。

### 退款规则

| 情形 | 处理 |
|---|---|
| 赛前 7 天以上 | 全额退 |
| 赛前 3–7 天 | 退 50% |
| 赛前 3 天内 | 不退 |
| 组织者取消赛事 | 全额退 |
| **候补到截止仍未转正** | **全额退**，不受赛前天数限制 |
| 换搭档 | 不涉及退款 |

> 候补那条不看天数：他从头到尾没打上球，按天数扣钱说不通。

---

## 5 · 签表

**签表是主办方赛前唯一要做的一屏。** 排签位、双打成组、人不够时补人，
全在这里，**不单开「组队」这一步**。

报名只产生一条单人 `Entry`；邀请只留下一条线索。剩下的都在签表上完成。

### 两种赛制

只做两种，覆盖约 90% 的业余赛事：

- **单败淘汰** → 树状签表
- **小组循环取前二出线** → 小组积分表 + 树状签表

赛制建赛时选定，决定签表长什么样。**「一盘打几局、平分怎么办」不在这里**，
那是比赛格式，见 §6。

### 选手能做的两件事

已报名的人有两个按钮：

| | 邀请队友 | 邀请朋友 |
|---|---|---|
| 意思 | **跟我一队** | **也来打这个比赛** |
| 落地页 | 邀请落地页 | 赛事详情页 |
| 对方报名后 | 记 `invitedBy = 我` | 记 `referredBy = 我` |
| 参与配对 | **是** —— 建议配对靠它 | **否** —— 只做来源统计 |

> **两个字段必须分开，这是正确性问题不是记账问题。**
> 共用一个的话，签表屏会把「张伟叫李强来打比赛」误读成「张伟想跟李强一队」，
> 生成一条**看起来很有道理的错误建议** —— 管理员多半就点确认了。
>
> `referredBy` 第一版**不做界面**，只把字段存下。冷启动阶段「人从哪来」
> 这类数据丢了就补不回来，而多存一个字段几乎不要钱。

**什么都不点也没关系** —— 待编排是默认状态，主办方会在签表上排。

### 接受邀请只记一个字段

点「接受」的人走的是**普通报名流程**：注册（若未注册）→ 付自己那一半 →
落进待编排池，**外加一条 `invitedBy`**。

从「邀请朋友」的分享链接进来的人，走的是完全相同的流程，
只是记的字段变成 `referredBy` —— 它不影响配对。

**不自动成组。** 两人仍是两条独立的单人 `Entry`，等主办方在签表上把它们并成一条。

> 这样做的好处是**只有一条报名路径**。接受邀请、扫码报名、朋友推荐进来的，
> 在数据上完全一样，区别只是来源字段填没填。
> 没有合并时序、没有并发抢位、没有第二套状态。

### 邀请队友的链接

**可群发，先到先得，接受即失效** —— 避免两个人都以为自己是我的搭档。

生成链接前三个入口，产出同一种链接：常搭档列表（排最上面，唯一不需要
输入就能选到人的入口）· 搜索昵称/手机号 · 直接转发。
**不做绑定给某个人的排他邀请** —— 与先到先得矛盾。

链接是**免登录的分享落地页**，未注册也看得到。点「接受」才撞登录墙 ——
弹注册页，注册完自动回来继续。这是业余赛拉新最主要的入口。

### 签表屏怎么用

**没有独立的「配对」环节。** 主办方在**网页后台的签表屏**上，
把待编排池里的两个人拖进同一个签位 —— 那一下**既是成组也是抽签**。

理由很直接：成组和抽签看的是同一批人、同一批信息（性别、自评水平、
谁想跟谁一队），先在一屏里两两配好、再到另一屏重排一遍签位，
**等于把同一件事做两遍**。

签表屏的构成：

| 区 | 放什么 |
|---|---|
| 左侧 **待编排池** | 所有已付款但还没进签位的人，按自评水平排序 |
| 右侧 **签表** | 签位树。双打一个签位放两人，单打放一人 |

进屏时**已经铺过一遍底**，不是空白开始：

- **有 `invitedBy` 关系的两人，预先并好放进池子**，管理员不用管
- **性别校验反过来用** —— 男双池里只有男性，混双拖错会挡下
- **按自评水平排序** —— 相近的挨在一起，随手拖就是合理的分组

> 这就是「机器铺底，人处理例外」：常见情形进屏时已经成对，
> 管理员只处理剩下的零头。少一屏，也少一次「刚配好又要重排」的返工。

**拖进签位的那一刻**，两条单人 `Entry` 并成一条，状态从待编排变成正选。
拖出来就拆回两条。

**单打不受影响** —— 一人就是一条 `Entry`，直接拖进签位。

### 人不够时，从俱乐部成员里补

报名人数凑不满一张签表是业余赛的常态。主办方可以在签表屏上
**直接从本俱乐部成员里挑人拖进签位**。

| | 规则 |
|---|---|
| 挑谁 | **本俱乐部已入会的成员**（`Membership` 为已入会，见 §2） |
| 要不要注册 | 要。成员本来就是注册用户 —— **没有占位账号这条红线不破** |
| 付不付钱 | **默认不付**。`Entry` 上记一个「主办方补入」的标记，账目上分得开 |
| 占不占名额 | 占。补进来的和正常报名的在签表上完全一样 |
| 性别校验 | 一样管用，男双池里挑不到女性 |

> **这是入会审批（§2）第一个真正的用处。** 成员名单不再只是个荣誉榜，
> 它是人不够时的**人源**——也是俱乐部愿意认真管成员的理由。
>
> 边界还是那一条：**补人只能补进签表，不能替人报名。**
> 被补进来的人在小程序上看到的是「主办方把你排进了这场比赛」，
> 不是一条他自己没点过的付款记录。

### 报名的满员状态不阻塞签表

抽签只认**已付款且在池子里**的人。所以：

| 情形 | 对签表的影响 |
|---|---|
| 名额满了 | **没有**。满员只是不再收新的正式报名 |
| 候补也满了 | **没有**。候补不进签表，转正了才进池子（转正是立刻的，钱已经收过） |
| 还有人没组上队 | **没有**。签表照抽，没进签位的人留在池子里 |

**报名截止时仍在池子里的人 → 作废并退款**（§4）。
主办方可以在截止前先抽一部分，剩下的等人凑齐再补 —— 签表不要求一次抽完。

### 第一版不做种子

**抽签就是随机**，抽完主办方可以手动拖着调。没有种子位、没有强弱分区。

> 理由是业余赛里种子多半算不准：自评水平人人往高了填，系统水平第一版
> 不算，上赛季积分在冷启动阶段根本没有。拿不准的排序会让签表**看起来
> 有依据其实没有** —— 那比明说「随机」更伤信任。
>
> 主办方对着签表拖两下，比任何算法都贴近他心里那张表。
> 种子列为第二版，见 §11。

### 轮空均匀分散

签位数取**不小于报名队数的最小 2 的幂**，多出来的就是轮空 ——
12 队打 16 签位，4 个轮空，那 4 队第一轮不用打直接进第二轮。

**轮空必须撒开，不能扎堆。** 轮空是白捡的便宜（少打一场、少受一次伤、
体力多一分），4 个全落进同一个 1/4 区，那个区两队白捡而另外三区全程打满 ——
主办方当场就会被人指着签表问。

规则：**按二分递归顺序占位**（也就是二进制位反转）。
把第一轮的场次编号后按这个顺序排，轮空依次占前 N 个：

| 签位数 | 第一轮场次 | 轮空占位顺序 |
|---|---|---|
| 16 | 8 场 | 1 · 5 · 3 · 7 · 2 · 6 · 4 · 8 |
| 32 | 16 场 | 1 · 9 · 5 · 13 · 3 · 11 · 7 · 15 · 2 · 10 · 6 · 14 · 4 · 12 · 8 · 16 |

效果是**任意个数的轮空都自动散开**：1 个在上半区，2 个上下半各一，
4 个刚好四个 1/4 区各一个。不需要为每种报名人数单独定一张表。

> 传统做法是把轮空给种子（保护强者）。第一版没有种子，所以改成
> 纯几何上的均匀 —— 谁拿到轮空是随机的，但**不会有某个分区系统性占便宜**。

### 自动生成一次，之后人说了算

「均匀分散」和「手动拖」不是二选一：

| | 管什么 |
|---|---|
| **自动生成** | 点一下，随机排 + 轮空按上面的顺序撒。**签表不从空白开始** |
| **手动拖** | 生成之后随便调。32 组全手动要拖 32 次，全自动又应付不了「这两对是师徒，别第一轮就碰」 |

两条配套规则：

- **拖过之后不再自动重排。** 系统不会「纠正」管理员的手动结果 ——
  他把一支队伍拖走，轮空跟着变，那是他的决定
- **重新生成会覆盖手动结果**，所以要二次确认。一句「重新生成会丢掉你
  拖过的位置」比事后撤销可靠

> 这就是「机器铺底，人处理例外」的第三次落地（前两次是成组和排程）：
> **自动负责让签表一上来就是合理的，人负责处理算法看不见的那几条约束。**
> 上面那条「轮空四个 1/4 区各一个」是**自动生成那一下**的规矩，
> 不是拖完之后还要守的不变量。

### 小组循环怎么分组

只在赛制选「小组循环」时适用。

#### 每组几队 —— 建赛时设，默认 4

**这是建赛表单里唯一跟签表有关的可配项**，按**比赛**设（一个赛事下的男双和
女双可以不一样，落在 `Event` 上）。

> **说「队」不说「人」。** 双打一队是两个人，「4 人一组」会被读成 4 个人、
> 也就是 2 队。单打一队就是一个人，那时才说「4 人一组」。见 `SPEC.md` §1 术语表。

12 队报名，两种分法差很多：

| | 分法 | 每队保底 | 小组赛场次 | 出线 |
|---|---|---|---|---|
| **4 队一组**（默认） | 3 组 × 4 队 | **3 场** | 18 场 | 6 队 → 8 签位，2 个轮空 |
| 3 队一组 | 4 组 × 3 队 | 2 场 | 12 场 | 8 队 → 正好填满 8 签位，不用轮空 |

默认 4 的理由：**业余赛的第一诉求是「来一趟要多打几场」**，每队保底 3 场比
2 场值钱得多。代价也写清楚：多 6 场比赛、多占场地时间，出线 6 队填不满
8 签位，会产生 2 个轮空（按上面的均匀分散规则撒）。

> 场地实在紧张时改 3 队一组；或者保持 4 队一组但**小组赛打短盘** ——
> 后者只让每场短一点，不减少上场次数。

#### 怎么分配 —— 随机 + 可手动拖

跟签表同一条规则（见上）：自动随机分完，管理员不满意就拖。

**同俱乐部要不要避开、师徒要不要错开，一律交给手动拖，不写进算法。**
这类约束算法看不见，写进去只会做出一个又复杂又猜不准的分组器。

> 第一版没有种子，所以**没有蛇形分组**的问题 —— 那本来就是给种子排的。

#### 组内排名 —— 平台固定，组织者不可配

按顺序比，前一条分不出才看下一条：

**胜场 → 净胜盘 → 净胜局**

**三方循环**是并列里最难缠的一种 —— 三队互相咬，每队都 1 胜 1 负：

| | 战绩 | 净胜盘 | 名次 |
|---|---|---|---|
| 张队 | 胜王 2:0 · 负陈 0:2 | **0** | 2（出线） |
| 王队 | 负张 0:2 · 胜陈 2:1 | **−1** | 3（淘汰） |
| 陈队 | 胜张 2:0 · 负王 1:2 | **+1** | 1（出线） |

光看胜场三家全平，净胜盘一比就分出来了。

> **就这三档，没有第四档。** 三档全平就是**并列** —— 系统不往下判，
> 表里并列显示，出线由主办方在签表上定（跟「不做聪明的自动处理，
> 判断留给人」是同一条，见 `SPEC.md` §1）。
>
> **尤其不抽签**：当众掷硬币输的人一定不服，而且事后没法复算 ——
> 谁也说不清那次抽的是什么。真走到三档全平是极罕见的边角，
> 为它多设一条全场都得记住的规则不划算。
>
> **这条不给组织者配**，理由和积分分值一样（§7）：赛场上临时改判定顺序，
> 就是在颁奖前吵架。规则事先写死、公开可查，才吵不起来。

#### 出线取前几 —— 第一版固定 2

**不给组织者配。** 它不是自由旋钮：`出线队数 = 组数 × 取前几`，
而这个乘积必须填得进 2 的幂签表。

| 12 队 · 4 队一组 | 出线 | 签表 | 结果 |
|---|---|---|---|
| 取前 **2**（默认） | 6 队 | 8 签位 | 2 个轮空，正常 |
| 取前 1 | 3 队 | 4 签位 | 小组赛最后一轮变成走过场 |
| 取前 3 | 9 队 | **16 签位** | **7 个轮空**，一张没法看的签表 |

取前 2 还有个好处：4 队一组淘汰 2 队，**绝大多数队打到最后一场都还有意义**。

> 字段留着（`Event.qualifyCount`），第二版遇到「8 组取第一」这种场景
> （8 队正好填满 8 签位）改一个值即可，不动模型。
> 它在 **Event 上不在 Group 上** —— 同一个比赛里所有小组必须一致。

#### 出线后怎么接进签表

**交叉**：A 组第一碰 B 组第二，不能 A 组第一碰 A 组第二 ——
同组的刚打过，第一轮再碰一次没意义。

出线队数不是 2 的幂时（比如 6 队进 8 签位），轮空照上面的均匀分散规则撒。

---

## 6 · 记分规则

### 比赛格式

**格式是配置，不是清单。** 建赛时给几个常用预设当快捷方式，但主办方能在
预设基础上改任意一项 —— 业余赛的格式因场地、天气、报名人数天天在变，
任何固定清单都会漏掉别人正在用的那一种。

六个维度彼此独立，按**盘 → 局 → 分**从大到小排 ——
跟人说赛制的顺序一致（「打两盘、一盘六局、六平抢七」）：

| | 维度 | 取值 |
|---|---|---|
| **盘** | 几盘几胜 | 1 / 2 |
| **盘** | 决胜盘抢到几分 | **0 = 打满一盘**；10 = 1-1 后打一个抢十 |
| **局** | 一盘打几局 | **6**（标准）· **8**（八局制 pro set）· **4**（短盘） |
| **局** | 几平进抢七 | 默认等于局数；**0 = 一开局就进**，整场就是一个抢十 |
| **分** | 抢七到几分 | 默认 4 局及以下抢五，6 局和 8 局都抢七 |
| **分** | 平分怎么办 | 金球（不打占先）/ 占先制 |

> 抢几分不用单独填，不传就按上面的默认推。
> **决胜盘那一项必填** —— 写 10 就是决胜抢十，写 7 就是决胜抢七，
> 写 0 就是照常打满一盘。不给默认值，因为两盘制和三盘制的赛程时长
> 差着一倍，这是建赛时必须想清楚的一项。

常用预设（只是快捷方式，字段清单见 `api/types.ts` 的 `MatchFormat`）：

| 预设 | 说明 |
|---|---|
| **两盘 + 决胜抢十 · 6 局金球 · 抢七** | **默认** —— 业余双打最主流，省时间、场地周转快 |
| 三盘两胜 · 6 局金球 · 抢七 | 正式一些的赛事 |
| 三盘两胜 · 6 局短盘 · 抢七 | 占先制，最接近职业规则 |
| 两盘 + 决胜抢十 · **4 局金球 · 抢五** | 大签表 / 场地紧张 |
| 单盘 · 6 局金球 · 抢七 | 小组赛常用 |
| **单盘 · 8 局金球 · 抢七** | 八局制 —— 六局嫌短、三盘两胜太长时正好 |
| 抢十 | 场地极紧张，整场就一个抢十 |

**「短盘」和「金球」是两个维度**，别并成一个开关 —— 短盘讲一盘打几局，
金球讲平分之后怎么办。同理，**「决胜盘抢十」和「整场一个抢十」不是一回事**。

> **存进 `Match` 的是解析后的配置对象，不是预设名。** 每场比赛按开打那天的
> 规则结算 —— 以后调整预设定义，历史比分不会被重新解释。
>
> 显示文案由配置**推导**（「6局金球 · 抢七 · 两盘 + 决胜抢十」），不能存 ——
> 主办方改过任意一项之后，存下来的那句话就是错的。

### 权限：一个开关，不是一套配置

- 默认**选手记分**，每场指定一名选手
- **管理员随时也能记**
- **管理员可以对某一场关掉「选手记分」**

> 建赛那天并不知道哪场会直播、哪场会吵起来。等那场要开打了再关，
> 才是真实的决策时机。

**管理员记的分不需要选手确认。** 管理员本来就有「改比分」的权力，让他记的
分再回去等选手批准是矛盾的；真出争议时，让吵架的一方去确认会直接卡死。

### 同时只有一个人在记

**一场比赛同时只有一个记分方。** 进记分器那一下就向服务端**认领**，
已经有人在记的，第二个人进不去 —— 界面告诉他是谁在记，只给「看比分」。

> **认领必须落到服务端，不能只在本地开。** 两个人各自在本地记，冲突要到
> 提交那一刻才暴露，那时候两份记录都已经存在，谁也说不清该信哪一份。
>
> **不给选手自助抢占。** 允许抢的代价是：前一个人离线记了半场，提交时才
> 被告知「你不是记分方了」—— 而记分是离线优先的，这个「才」可能是一小时之后。
>
> **手机没电这类真实情况走管理员接管** —— 管理员本来就随时能记（上面那条）。
> 让人去找管理员，比给一个谁都能按的「接管」按钮安全得多。

**认领失败不阻止记分。** 开赛前那一下没网（球场信号差是常态，
离线优先是地基），照样进得去，界面挂一条「未认领」；联网后补认领。
补认领撞上别人已经占了，**那时才提示，而且本地那份记录不丢** ——
让他能把它交给记分方，或者上报管理员。

**记分方可以主动退出**，把记分权交回去让别人接手。退出交出去的是
**记分权，不是这场比赛** —— 比分留着，下一个人接着往下记。

> 这是「不给自助抢占」那条的**配套出口**。只锁不给退出，记分方临时有事
> （接个电话、自己要上场了）就把整场卡死了 —— 那比允许抢占还糟。
>
> ⚠️ **退出前必须先把本地的分同步上去。** 先交权后同步，接手的人从服务端
> 拿到的是一份旧比分，中间那几分谁也找不回来。所以没网时这个按钮点不动，
> 上面写「未同步，先联网」。
>
> **退出不等于返回。** 点左上角返回只是离开这一屏（去看眼签表再回来），
> 记分权还在他手上。交权必须是显式的一下。

### 不记小分也能结束比赛

**逐分记不是唯一的路。** 业余赛的常态是没人愿意每分点一次 —— 打完回来
填一个 6-4 6-3 就想收工。所以比分板上的盘分格子**本来就能直接点着填**，
填够了照样能结束、照样推进签表。

> **不是另一种模式，也不另开一屏。** 上面两块点击区、下面可填的比分板，
> **两样一直都在**。做成模式开关，人还得先决定「我今天用哪种」——
> 而那个决定往往到第三局才做得出来。开赛前也不问这一句。

| | |
|---|---|
| 填什么 | 按盘填局分；7-6 那种要补上抢七的分 |
| 什么算填够 | 按这场的赛制判 —— 赢够盘数就是结束 |
| 能不能混着来 | 能。前两盘逐分记、第三盘直接填，是常见情形 |

**任何时候都能结束并提交，只要指明谁赢。** 对手没来、天黑了、场地到点、
伤退 —— 业余赛里没打满的场次是常态。所以「结束比赛」**永远是可点的**，
比分填到哪算哪，缺的那些盘就是空的。

| | |
|---|---|
| 必须有的 | **获胜方**。一个都没填的比分也能提交，但必须说清谁赢 |
| 可以缺的 | 比分。打了半盘、一分没记，都提交得上去 |
| 仍然要合法 | **填了的那部分**。6-5 还是不能填 —— 缺可以，错不行 |

> **缺获胜方比缺比分危险得多。** 场次照样变成待确认，确认之后签表要推进时
> 找不到胜者，**就静默卡在那儿** —— 而界面上一切正常，没人查得出来。
> 所以这一条在云函数里是硬拦的（`BAD_ARGS`），守卫在
> `test/cloud.test.js` `[7e]`。
>
> **「比赛获胜者」常驻在结束按钮上面，不做二次确认弹层。** 结束一场只需要
> 这一件事，做成弹层等于把唯一的必填项藏到点了按钮之后 —— 人得先点一下
> 才知道还要选。摆在外面，一眼就知道离结束还差什么；没选时按钮压暗。
>
> 比分能判出胜者时**预选好**，标签换成「已按比分选好，可以改」——
> 别让人再答一遍已知的问题，但也别锁死：打到一半对手伤退，比分和结果
> 本来就可以不一致。

**盘位给的是选项，不是输入框。** 点开第 1 盘，列出来的就是这个赛制下打得
出来的那几个（6 局制：`6-0 6-1 6-2 6-3 6-4 7-5 7-6`）。选 7-6 会接着问抢七
打到几比几 —— 少了那个分，7-6 分不出谁赢。

名单由 `utils/tennis.js` 的 `legalSetScores` 按赛制算出来，不是写死的表：
八局制是 `8-0 … 8-6 9-7 9-8`，4 局制是 `4-0 4-1 4-2 5-3 5-4`。

> `7-0` 这种「看着合法」的比分**永远走不到** —— 拿到第 6 局领先 2 局，
> 那盘当场就收了。名单里没有它。

> **把错误变成填不出来，比把错误提示写漂亮有用得多。** 6-5 不在名单上，
> 所以「填错了」这个状态压根不存在，界面上也就不需要那条红色报错。

**但判据仍然要留着**，只是换了岗位：从「界面报错的依据」变成**云函数那一侧
的守卫**。选项名单是好客户端的事，服务端不能假设客户端是好的 —— 导入的历史
数据、管理员改比分、以后别的端，都从那儿进来。判据在
`mp/miniprogram/utils/tennis.js` 的 `resultFromSets`，守卫在
`test/tennis.test.js` `[11b]`。没有它，签表会被一个打不出来的比分推进，
而且**事后没人查得出来**。

> **代价要说清楚：不记小分，就没有小分。**
> 直播面板显示不了 15/30/40（`Match.liveNow` 一直是空的），只能显示局分；
> 以后想做的发球统计也没有数据来源。
>
> **这是一个选择，不是缺陷** —— 但界面上得让人知道自己在选什么，
> 别等到开播那天才发现面板上只有局分。

### 确认流程

```
选手记分 → 结束自动生成确认卡 → 对方组合任一人 1 tap 确认 → 比分生效
                                      ↓ 有争议
                                 冻结，上报管理员裁定
```

未确认的比分在签表和战绩里要有可见的状态标记。

### 直播比分面板（OBS）

给直播用的**网页端只读面板**：OBS 的 browser source 加载一个 URL，
实时显示某一场次的比分。

| | 规则 |
|---|---|
| 在哪 | **网页端**，不是小程序 —— OBS 加载的是 URL |
| 怎么调出某一场 | **输场次编号**，一个输入框。不生成链接、不用登录 |
| 编号长什么样 | `260910-0012` —— 日期 + 4 位序号，全局唯一（`api/types.ts` 的 `Match.code`） |
| 显示 | 双方名字 · 已完成盘的局分 · 本局当前分 · 谁在发球 |
| 权限 | 只读，谁都能看，不能改分 |

> **不做「管理员生成链接、链接可撤销」那一套。** 直播的人多半不是组织者本人，
> 让他输一串能口头念、能在微信里发的数字，比走一遍生成 - 复制 - 粘贴短得多。
>
> 代价是**编号能猜**（日期 + 序号）。面板本来就是公开只读的，所以可接受 ——
> 但**编号只在 `live` 状态下调得出来**，比赛一结束就查不到，
> 免得它变成一个能翻遍历史场次的口子。撤销就是把那场停掉。

> ⚠️ **不能反过来要求记分器在线。离线优先是地基，动了它一切归零。**
>
> 冲突是真的：面板要逐分实时，而 `MatchScore` **只存每盘比分，不存逐分**
> （见 `SPEC.md` §3）。解法是不改最终存储 —— 记分器在 `live` 状态下**额外**
> 往一个易失字段推「当前分 + 发球方」，面板订阅那个字段。
>
> 断网时记分照常在本地进行，只是面板停在最后一次同步的比分上，
> 恢复网络后自己追上。**面板降级，记分不降级。**
>
> 出口本来就在：**直播场次天然有网**（没网也直播不了）。

### 双打发球轮转

记录到**「谁在发球」**的粒度 —— 这是双打里最有价值的个人数据（能算保发率）。

- **双打**开局设定一次：A1 → B1 → A2 → B2（2 tap），之后每局自动轮转
- **单打**是两人轮流，每局换发，无需设定
- 用户全程零操作
- 抢七内第 1 分后换发，之后每 2 分换一次（ITF 规则）

---

## 7 · 积分

### 组织者选等级，平台定分值

| 等级 | 定位 |
|---|---|
| A | 市级以上公开赛 |
| B | 俱乐部公开赛 |
| C | 内部友谊赛 |

各等级的轮次分值由平台固定。**具体数字待定，不阻塞画页面。**

> 为什么不让组织者自由填分值：**全局榜会失去可比性**。赛事 A 冠军给 1000、
> 赛事 B 给 100，榜首可能只是参加了分值虚高的赛事。ATP 榜单成立恰恰因为
> 分值全局统一 —— 1000/500/250 是等级，不是自由输入框。

### 榜单

- 排**人**不排组合 —— 搭档会换，排组合没有连续性
- 默认**全局**（「全城第几」是更强的动机），可按俱乐部筛选
- 筛选维度：**类型 / 赛季 / 俱乐部**

**「按俱乐部筛选」筛的是赛事，不是人。** 只统计在该俱乐部办的赛事里拿到的
积分 —— 选手可加入 0 个或多个俱乐部，归属是社区层，与积分无关。
**榜单行不显示俱乐部。**

榜单行：名次 · 昵称 · 积分 · **参赛场次**。
场次必须显示，否则「打得多分就高」会让榜单显得不公。

---

---

## 8 · 三个数字

| | 积分 | 自评水平 | 系统水平 |
|---|---|---|---|
| 字段 | — | `selfRatedLevel` | `systemRatedLevel` |
| 怎么来 | 参赛累加 | 注册时拖滑杆 | 从比分推算 |
| 用户能改 | ✗ | ✓ 随时 | ✗ |
| 会不会降 | 不降（赛季清零） | — | 会升会降 |
| 第一版 | ✅ 上榜 | ✅ | ⬜ 字段留位，算法延后 |

**两个水平共用一把尺子 —— NTRP 2.0–5.0。** 自评卡 0.5 一档
（1.5 实际没人自评到，不放出来），系统值是连续的（`3.4`）。

**刻度必须相同**，因为界面上它俩并排显示。两套刻度并排（比如自评 3.5、
系统 7.2）没人读得懂 —— 用户第一反应是「我到底是几」。

自评只做赛事初始分组，不依赖算法。

界面上并排显示，标签写全「自评」「系统」。
**系统值没算出来时显示「—」，绝不用自评顶替** ——
否则用户打输三场发现数字没动，会认为系统坏了。

---

---

## 9 · 注册

字段：**昵称 · 手机号 · 性别 · 水平**

| 字段 | 交互 |
|---|---|
| 昵称 | `<input type="nickname">` —— 点输入框，微信在键盘上方推候选。**不弹授权、不收费、个人主体可用**，是 `getUserProfile` 废弃后唯一的路子 |
| 手机号 | **只能微信授权**（`getPhoneNumber`），已去掉手动输入 |
| 性别 | 分段，默认男。**注册后用户改不了** —— 提交前要提示这一点，选错只能找组织者 |
| 水平 | 刻度滑杆，默认 2.5 |

**触发点两个**：点「报名 / 接受邀请」时，或点「我的」tab 时。

> 手机号在云函数怎么换、`code` 为什么必须当场用掉 —— 见 `SPEC.md` §4。
> 注册页的底图和视觉 —— 见 `design/DESIGN.md` §3。

### 手机号：谁能看到全号

**全号从不下发给选手。** 选手侧任何地方看到的都是掩码 `138****6421` ——
包括自己的。真号只在云函数里存在。

主办方要联系选手是**真实需求**（临时换场、下雨改期、找不到人），所以
后台能拿到全号，但受三条约束：

| | 规则 |
|---|---|
| 谁能看 | **只有该赛事的俱乐部管理员**，且只能看**自己赛事**报名者的号 —— 不是「管理员能看所有人」 |
| 什么时候给 | **按需**：点开某一条报名才返回那一个号，列表页一律只给掩码 |
| 留不留痕 | **每次读全号记一条操作日志**：谁、什么时候、看了谁的号 |

> 第三条最容易被当成可有可无。真出事（号码被拿去推销）的时候，
> **操作日志是唯一能查的东西** —— 没有它，你连是不是内部流出都说不清。

**存储要加密。** 《个人信息保护法》要求手机号这类个人信息采取加密等
安全技术措施；明文存一个真实用户的手机号库，备案和后续审查都是隐患。
具体方案见 `SPEC.md` §4。

---

## 10 · 上线门槛（硬性前提）

这三条**写代码解决不了**，且都以「周」计，要提前并行启动：

| 前提 | 卡住什么 |
|---|---|
| **企业主体** | 微信支付 **和** 手机号快速验证，两者都要 |
| **用户隐私保护指引过审** | 未过审 `getPhoneNumber` 直接不可用，报 `api scope is not declared`（不是权限错误，容易误判）。后台路径：设置 → 基本设置 → 服务内容声明 → 用户隐私保护指引 → 勾选「手机号」 |
| **商户号** | 报名费支付。没有企业主体办不了商户号 |

另：手机号快速验证组件 **2023-08-26 起按次收费**，每次成功约 0.026–0.03 元。

> **`me/settings` 现在没有入口。**「我的」页上的「设置」行按评审去掉了
> （见 `design/me/`）。**账号注销是合规要求，上线前必须给它找个落点** ——
> 最省事的是塞进「资料编辑」里，不必为它单开一行。

---

---

## 11 · 延后但要留接口的

**算出来的水平（`systemRatedLevel`）。** 字段留位，算法不做 —— 见 §8。
界面上凡是要显示它的地方一律显示「—」，**绝不用自评顶替**。

**种子。** 第一版随机抽签（§5）。第二版再做，届时要定的是：
按什么排序（系统水平？上赛季积分？主办方手填？）、几个种子、
种子之间怎么分区。

> 留的接口是签位本身：签表结构里给每个签位留一个可选的 `seed` 序号，
> 第一版一律为空。**加种子不改签表模型，只是多一步排序。**

> 直播比分面板原来在这一节，**已移进范围**，见 §6。移得动的原因正是
> 当初留了接口：记分权限定得宽（管理员随时能记 + 单场开关），
> 加一个只读消费端不必改模型。

---

## 附 · 优先级与验收标准

### P0 · 不做就跑不完一场赛

判据只有一条：**抽掉它，那场 16 队的男双就走不到出积分。**

| # | 能力 | 在哪 | 验收标准（可勾） |
|---|---|---|---|
| 1 | 注册 | `auth/register` | 新用户能拿到微信手机号并落库，真号不出服务端 |
| 2 | 建赛 | 网页后台 | 建出一场带 A/B/C 等级、报名截止、名额、赛制的男双；选小组循环时能设每组队数 |
| 3 | 报名 + 付款 | `entry/create` | 付款成功后 `Entry` 落在待编排；满员时进候补，**同样收款**，须知里写明可能打不上 |
| 4 | 名额准确 | 云函数 | 16 队的比赛并发报名，**第 17 队必定进候补** ✅ 已修 |
| 5 | 签表 | 网页后台 | 把两人拖进同一签位即成一队；性别校验挡住错配；12 队**自动生成**一张 16 签位单败签表且 4 个轮空落在四个 1/4 区；生成后可手动拖调且不被自动纠正；人不够时能从俱乐部成员里补 |
| 6 | 排程 | 网页后台 | 每场次能落到场地 + 时间，选手端看得到 |
| 7 | 记分 | `score/live` | **断网可用**；一分 1 tap；抢七、**金球平分后一分定胜负**、**1-1 后决胜盘抢十**都算得对 |
| 8 | 确认比分 | `score/confirm` | 对方任一人 1 tap 确认后比分生效；有争议能冻结 |
| 9 | 出积分 | 云函数 | 赛事结束后按等级和轮次累加，榜单能看到 |
| 10 | 截止结算 | 定时任务 | 报名截止时：未进签位的作废退款、未转正的候补全额退款 |

> **4 和 7 是这张表里最容易被低估的两条，现在都修完了。**
> 4 的判据从「只数 `confirmed`」改成「`seeking_partner` + `confirmed`」；
> 7 补上了金球和决胜盘抢十 —— 原来那个扁平枚举两样都表达不了。
> 守卫分别在 `test/entry.test.js` `[3b]` 和 `test/tennis.test.js` `[2b]`。

### P1 · 第一场赛之后补

不影响闭环，但少了会让人不舒服：

候补自动转正 · 自助申请退款 · 邀请队友链接 · 我的战绩 ·
积分榜筛选（类型/赛季/俱乐部）· 代录线下报名 · 改比分 · 资料编辑 ·
**入会审批** · **直播比分面板**

> 这两条新进来的都是 P1 不是 P0：抽掉它们，那场男双照样能从报名走到出积分。
> 入会审批不挡报名（§2 的红线），直播面板只是多一个只读消费端。

### P2 · 明确延后

`systemRatedLevel` 算法（字段留位）· 水平上榜 · **种子** ·
`referredBy` 的界面 · 常搭档推荐

延后的理由见 §1「明确不做」和 §11。**延后不是否决** —— 接口留着。

### 怎么用这张表

1. 交付时按 P0 的验收标准逐条勾，勾不满不叫做完
2. 有人提新需求，先问它属于 P0/P1/P2 —— 说不出来的，多半是 P2
3. P0 里有一条做不动，**砍范围不砍质量**：宁可第一场只办男双，
   也不要十一条都做一半
