  <style>
    :root{
      --c-ground:#F4F5F7; --c-surface:#FFFFFF; --c-ink:#141821; --c-ink-2:#4C5567;
      --c-ink-3:#7B8496; --c-line:#DFE2E9; --c-brand:#2E4FD8; --c-brand-ink:#FFF;
      --c-brand-weak:#E7EBFC; --c-live:#F5A524; --c-win:#0E9F6E; --ann:#B03A2E;
    }
    *{box-sizing:border-box}
    body{margin:0;padding:30px 34px;background:var(--c-ground);color:var(--c-ink);
      font-size:13px;line-height:1.45;
      font-family:-apple-system,"PingFang SC","Source Han Sans CN",system-ui,sans-serif}
    a{color:var(--c-brand)}a:hover{color:#243FAD}
    .h1{font-size:21px;font-weight:700;letter-spacing:-.01em;margin:0 0 3px}
    .sub{color:var(--c-ink-3);font-size:12px;margin:0 0 22px}
    .cols{display:grid;grid-template-columns:1.15fr .72fr .95fr;gap:30px;align-items:start}

    .root{display:inline-flex;align-items:center;gap:8px;background:var(--c-brand);
      color:var(--c-brand-ink);border-radius:8px;padding:8px 14px;font-weight:600;font-size:14px}
    .root .p{font-family:ui-monospace,Menlo,monospace;font-size:10.5px;opacity:.8;font-weight:400}

    /* 树线 */
    .tr{position:relative;margin-left:15px;padding-left:20px}
    .tr::before{content:"";position:absolute;left:0;top:0;bottom:13px;border-left:1px solid var(--c-line)}
    .it{position:relative;margin-top:7px}
    .it::before{content:"";position:absolute;left:-20px;top:13px;width:16px;border-top:1px solid var(--c-line)}

    .b{display:inline-flex;align-items:center;gap:8px;background:var(--c-surface);
      border:1px solid var(--c-line);border-radius:6px;padding:5px 10px}
    .b.sub{border-style:dashed;background:transparent}
    .b .n{font-weight:600;font-size:12.5px;white-space:nowrap}
    .b .p{font-family:ui-monospace,Menlo,monospace;font-size:10px;color:var(--c-ink-3);white-space:nowrap}
    .d{width:6px;height:6px;border-radius:999px;flex:none}
    .d.free{background:var(--c-win)}
    .d.org{background:var(--c-live)}

    .float{margin-top:26px;padding-top:16px;border-top:1px dashed var(--c-line)}
    .float .cap{font-size:11px;color:var(--c-ink-3);margin-bottom:9px}

    .lg{margin-top:26px;padding-top:14px;border-top:1px solid var(--c-line);
      display:flex;gap:20px;flex-wrap:wrap;font-size:11.5px;color:var(--c-ink-2);align-items:center}
    .lg i{display:inline-block;width:22px;height:13px;border:1px solid var(--c-line);
      border-radius:3px;background:var(--c-surface);vertical-align:-2px;margin-right:5px}
    .lg i.sub{border-style:dashed;background:transparent}
    .anns{margin:18px 0 0;padding:0;list-style:none;display:grid;gap:6px;max-width:1000px}
    .anns li{font-size:12px;color:var(--c-ink-2);display:flex;gap:8px;align-items:flex-start}
    .anns li > b{flex:none;width:16px;height:16px;border-radius:999px;border:1px solid var(--ann);
      color:var(--ann);font-size:9.5px;display:grid;place-items:center;margin-top:2px;font-weight:400}
  </style>
