# LJTennis 技术规格

> 数据模型 · 页面 · 状态 · 实现约束。**产品规则不在这里，在 `PRD.md`。**
>
> 哪份文档管什么、冲突时以谁为准 —— 见根目录 `CLAUDE.md`「规格写在哪」，
> 那张表是唯一的一份，这里不重抄。

---

## 1 · 数据模型与术语

```
俱乐部 Club
 ├─ 会员 Membership        （申请中 / 已入会 / 已拒绝 / 已移除）
 └─ 赛事 Tournament        （带 A/B/C 等级）
     └─ 比赛 Event          （男双 MD / 女双 WD / 混双 XD / 男单 MS / 女单 WS）
         ├─ 参赛主体 Entry  = 1~2 名选手
         ├─ 签表 Draw       （一个比赛一张。签位数组，长度是 2 的幂）
         ├─ 小组 Group      （只有小组循环才有）
         └─ 场次 Match      （对阵双方是 Entry，不是人）
```

> `Membership` 是**旁挂**的，不在报名那条链上 —— 这是故意的，见下面
> 「选手与俱乐部」。

**`Entry` 是关键抽象**：单打时 1 人，双打时 2 人。签表、积分、记分全部挂在
Entry 上，单双打共用一套结构。

`Event` 上的赛制相关字段（建赛时设，选手不可改）：

| 字段 | 值 | 说明 |
|---|---|---|
| `format` | 单败淘汰 / 小组循环 | 决定签表结构 |
| `groupSize` | 4（默认）/ 3 | 每组几**队**，**只在小组循环时有意义**，见 `PRD.md` §5 |

> 组内排名的**判定顺序不是字段** —— 平台写死，组织者不可配，
> 规则见 `PRD.md` §5「组内排名」。

### 一个赛事可以挂多个比赛

男双、女双、混双同时办是常态，**不拆成三个赛事** —— 它们共享场地、日期、
俱乐部和一张封面。选手报名时**挑一个**自己要打的。

`Entry`、签表、积分都挂在 `Event` 上，所以各比赛之间完全独立：
男双满员不影响女双，男双的签表也不会跟混双混在一起。

### 签表怎么存

一个比赛一张签表（`Draw`）。核心就一个数组：

```
Draw.slots  长度 = size（2 的幂），下标就是签位号
            值是 Entry.id，null 是空签位

            第一轮第 k 场 = slots[2k] vs slots[2k+1]
```

**相邻两个签位就是一场**，所以没有单独的「签位」表，也没有「第一轮场次」表。
拖签位就是改这个数组里的两个下标。

| | |
|---|---|
| 一个签位放什么 | **一条 `Entry`**（双打 2 人、单打 1 人），不是一个人 |
| 签位数怎么定 | 不小于参赛队数的最小 2 的幂。12 队 → 16 |
| 轮空在哪 | 就是**空签位**，不另存清单 |
| 谁是真相 | `slots`。`Match` 是定稿时按它生成的**产物** |

**`slots` 是真相，`Match` 是产物。** 拖签位改数组，定稿那一下才生成场次；
反过来去改 `Match` 的对阵**不算改签表** —— 两处会对不上，第二天谁也说不清
哪份是对的。

#### 两处容易做错的

**一、`null` 的含义取决于 `Event.status`。**

| | `slots[i] === null` 是什么意思 |
|---|---|
| `drawn` 之前 | 还没排到那儿 —— 签表不要求一次抽完（`PRD.md` §5） |
| `drawn` 之后 | **轮空** |

不另存一份轮空清单：两处存必然不一致，而且「轮空」本来就是「定稿时还空着的
签位」这句话的意思，不是一个独立的事实。

**二、轮空不是一场 `Match`。** 签位对里有空位时，那场**压根不生成**，
另一边直接落到下一轮的 `entryAId`，上游场次留空。所以：

- 不需要 `isBye` 字段
- **别拿 `walkover` 顶** —— 弃权是人退赛（要处理、可能要退款），
  轮空是签位没填满（什么都不用做）。混在一起，「记分覆盖率」那个指标先出错

连带的好处是 `sourceMatchAId` / `sourceMatchBId` 同时成了「待定 vs 已定」
的判据：有上游 = 待定（显示「场次 11 胜者」），没上游而 entry 已填 = 已定。

#### 三个不起眼但少不了的字段

| 字段 | 为什么非有不可 |
|---|---|
| `generatedAt` | 没有 = 还没生成过。「轮空按二进制位反转撒开」**只在生成那一下成立**，不是拖完之后还要守的不变量 —— 别写定时任务去「纠正」它 |
| `handEdited` | **只有一个用途**：触发「重新生成会丢掉你拖过的位置」那句二次确认。系统不拿它去纠正管理员 |
| `version` | `slots` 是整个数组一起写的。两个管理员同时拖同一张签表，没有乐观锁就是静默覆盖掉对方拖了半天的位置 |

#### 小组循环只多一段，不多一张签表

`group_knockout` 也只有这一张 `Draw`，它是**出线之后**那半张。
小组阶段在 `Group` / `GroupStanding` 里，两段靠出线交叉衔接 ——
而交叉是**算出来的、不给拖**（`PRD.md` §5），所以那种赛制下
`slots` 由系统写，管理员不碰。

> 字段一级的注释在 `api/types.ts` 的 `Draw` 上，那份是真身。

### 系统只管收报名

**不做聪明的自动处理。** 名额、成组、异常一律走人：

| 情形 | 谁来处理 |
|---|---|
| 成组 | 主办方在签表上拖 |
| 配不上、退赛、临时缺人 | 主办方决定 —— 该退款退款，手动选择俱乐部会员参加 |
| 超时 | 只做自动作废与退款这两件保底动作 |

> 这条解释了今天很多决定为什么长这样：手动配对。
> **系统负责把钱和名额记清楚，判断留给人。**

### 术语表 · 界面上必须这么叫

| 模型 | 界面用词 | 例子 | 绝不能叫 |
|---|---|---|---|
| `Tournament` | **赛事** | 春季双打赛 | — |
| `Event` | **比赛** | 男双 · 女双 · 混双 | ~~项目~~ |
| `Match` | **场次** | 场次 12 | ~~比赛~~ |
| `Entry` | **报名** / 参赛 | 张伟 / 李强 | ~~组合~~（单打只有一人） |
| `Entry` 的**计数单位** | 双打 **队**、单打 **人** | 16 队 · 16 人 | ~~组~~（「组」留给小组循环） |
| `EntryStatus.confirmed` | **正选** | 正选 13 / 16 队 | ~~已确认~~（那个词留给比分） |
| `EntryStatus.seeking_partner` | **待编排** | 正选 · 待编排 | ~~待组队~~ |
| `Draw` | **签表** | 男双签表 | ~~抽签表~~ |
| `Membership` | **会员** | 已入会 | ~~会籍~~ |

> **改的只是中文标签，枚举值一个都不动。** 「场次」也不是新造的词 ——
> 签表和排程本来就在用（「场次 12」）。
>
> 交了钱之后只有**正选**和**候补**两种身份，「待编排」是正选身上的一个
> 待办而不是第三种身份 —— 规则的定义处在 `PRD.md` §4。

### 选手与俱乐部

`Membership` 挂在 `Club` 和 `User` 之间，带状态：

| 状态 | 含义 |
|---|---|
| `pending` | 申请中，等管理员批 |
| `active` | 已入会 |
| `rejected` | 被拒，可再申请 |
| `removed` | 被移除，可再申请 |

**入会要审核，报名不要。** 规则和红线见 `PRD.md` §2「入会与审批」——
`Entry` 不查 `Membership`，两条链路完全不相交。

---

## 2 · 页面清单

两套界面各自成章：**小程序 26 页**（选手 + 现场的组织者），
**网页后台 9 屏**（赛前的组织者）。分界线是「现场 vs 赛前」，
理由在 `PRD.md` §2。

### 小程序 · 26 页

#### tabBar 三项：赛事 / 积分 / 我的

`tabBar` 是 `app.json` 静态配置，所有人看到的一样。
**不给组织者开 tab**，组织者入口在「我的」页里。

#### 主包 17 页

三个 tab（`tournament` `rank` `me`）· `auth/register` ·
`tournament/detail` · `event/detail` · `club/detail` ·
`entry/create` · `entry/partner` · `entry/invite` ·
`draw` · `draw/overview` ·
`score/setup` · `score/live` · `score/confirm` ·
`org/match` · `me/entries`

| 页面 | 干什么 |
|---|---|
| `entry/create` | **报名确认** —— 费用、须知、协议 → 唤起微信支付 |
| `entry/partner` | **邀请队友** —— 常搭档 / 搜索 / 直接转发，生成邀请链接 |
| `entry/invite` | **邀请落地页** —— 被邀请的人看的，免登录 |

> 原来的 `entry/pay` 删掉：付款用 `wx.requestPayment` 在当前页唤起，
> 不需要独立页面。候补转正后的付款从「我的报名」直接唤起。
>
> 原来的 `entry/create`（发起报名选搭档）拆成两页 ——
> 报名时不再选人，选人是报名之后的独立动作。

#### 分包 9 页

`me/records` · `me/level` · `me/profile` · `me/settings` ·
`player/detail` · `rules/points` ·
`org/index` · `org/tournament` · `org/entries`

#### 进主包的判据

只要占**一条**就进：

1. **分享落地页** —— `tournament/detail` `event/detail` `club/detail`
2. **球场里要用** —— `draw` `score/*` `org/match`（信号差，分包下不下来）
3. **高频** —— 三个 tab、`me/entries`

> `org/match` 进主包的理由和记分器完全一样：**组织者也站在球场边**。

#### 未注册时的 tab 策略

| tab | 未注册 | 理由 |
|---|---|---|
| 赛事 | 不拦，完整可浏览 | 获客前提，分享落地页要能看 |
| 积分 | 不拦 | 榜单是公开信息 |
| 我的 | **拦**，显示未注册态页面 | 整屏都是个人数据 |

「我的」用**未注册态页面**而不是硬跳转 —— tab 页硬跳注册会把人困住退不
回去，还会打乱 tabBar 状态。

> **赛事与积分两个 tab 压根不区分注册状态。** 赛事首页只关心「我的报名
> 列表空不空」，未注册的人和已注册但没报过名的人看到的完全一样 ——
> 一个状态覆盖两种人，不需要为「访客」单独做一套。
>
> 判据：**拦不拦看内容属谁，不看用户是谁。**

注册页要携带**来源与回跳目标**。跨 tab 时走本地存储 ——
`wx.switchTab` 带不了参数。

> ⚠️ **`club/detail` 上的入会状态按钮**（申请加入 → 审核中 → 已入会）
> **还没出稿**。入会审批进范围之后它才出现，是这 26 页里唯一没有设计稿的一处。

### 网页后台 · 9 屏

桌面端，`adminweb/`。设计稿 `design/admin/`，工程约定 `adminweb/CLAUDE.md`。

| # | 屏 | 干什么 |
|---|---|---|
| 1 | 扫码登录 | 微信扫码换 session |
| 2 | 选择俱乐部 | 进后台的第一个岔路；平台管理员在这里建俱乐部 |
| 2b | 创建俱乐部 | 第 2 屏的分支表单：指定创建者 + 按字段填资料。**只对平台管理员出现** |
| 3 | 赛事列表 | 该俱乐部的全部赛事 |
| 4 | 入会申请 | 审批加入俱乐部的申请 |
| 5 | 新建赛事 | 建赛事 + 挂比赛，赛制和比赛格式在这里定 |
| 6 | 报名名单 | **先选比赛**，再看该比赛的报名、补人、导出 |
| 7 | 签表 · 单败淘汰 | 拖签位、轮空、手动改 |
| 8 | 签表 · 小组循环 | 分组、拖队、看组内排名 |
| 9 | 排程 | 场地 × 时段排场次 |

> **2b 不单独编号成第 10 屏**，它是第 2 屏点「创建俱乐部」之后的表单，
> 不在侧栏里、也不是独立入口。
>
> 建完的俱乐部**不会出现在建它的人自己的列表里** —— 第 2 屏列的是
> 「我管理的」（`Membership` 里 owner / admin 那几条），而平台管理员建店不入店。
> 这是「两层身份刻意不互相包含」的直接后果，成功提示里要说清楚。

**没有记分屏，是故意的。** 记分、改比分、处理争议都在小程序里 ——
人站在球场边，手上只有手机。后台屏大不是把现场功能挪过来的理由。

**没有身份切换器。** 侧栏底部一个「退回选俱乐部」就够了，
当前俱乐部名在顶部面包屑里已经有。

> 这些屏里**只有第 1 屏能在没有 `admin.*` 鉴权的情况下存在**。
> 其余全部读写俱乐部数据，其中第 6 屏还要显示手机号 ——
> 先补鉴权，再写页面，见 §5.2。

---

## 3 · 状态与枚举

**枚举的真身在 `api/types.ts`**，那份过 `tsc --strict`。
这里只说它们的含义与存储粒度；改枚举先改 `types.ts`。

### 报名状态

`EntryStatus`：`seeking_partner` · `waitlisted` ·
`confirmed` · `cancelled` · `refunding` · `refunded`

含义与迁移规则见 `PRD.md` §4。**界面上 `confirmed` 叫「正选」**，不叫「已确认」
（§1 术语表）—— `MatchStatus` 里另有一个 `confirmed`，那个才是「已确认」，
指的是比分。两个别串。

### 场次状态

`MatchStatus`：`pending` · `live` · `pending_confirm` · `confirmed` ·
`disputed` · `walkover`

未确认的比分在签表和战绩里要有可见的状态标记。

### 存储粒度

`MatchScore` 只存 **`sets` + `serveOrder`**，**不存逐分数据**。

> 这个取舍有两次回报：记分引擎能写成纯函数；
> 「改比分」只是编辑最多三盘局分，是个小屏不是重屏。

**记分全程离线可用**，签表进主包 —— 球场信号差。

签表本身怎么存见 §1「签表怎么存」。

---

## 4 · 实现约束

### 手机号

字段与触发点见 `PRD.md` §9；这里只写实现上绕不过去的四条。

- 客户端只拿得到 `code`，**真号必须在云函数换**
- `code` **有效期 5 分钟且一次性** —— 所以**授权当场就换**，
  不能攥着等用户填完表提交
- 换出来的真号存服务端，**只把掩码回给前端**
- `register` 只认服务端暂存，不接受客户端传号码充当「已验证」——
  否则谁都能凭空认领任意号码

#### 存储：现在是明文，字段名在撒谎

`users.phoneEncrypted` 这个字段**存的是明文**。**名字比明文本身更危险** ——
下一个人看到 `Encrypted` 会默认它安全，跳过检查。

要做的方案（产品侧规则见 `PRD.md` §9）：

| | |
|---|---|
| 算法 | 对称可逆 —— 需求是「联系客户」，哈希/不可逆脱敏直接出局 |
| 密钥 | 云函数**环境变量**，不进版本库、不进代码 |
| 字段 | 加密后字段名才配叫 `phoneEncrypted`；在那之前应该叫 `phone` |
| 解密 | 只在「管理员查看单条报名」这一条路径上，且要记操作日志 |

**这个方案挡什么、不挡什么要说清楚**：密钥和数据在同一个云环境里，
云函数被攻破就两样都有。它挡的是**数据库被单独拖走 / 误导出 / 运维误看** ——
现实中最常发生的正是这三种，所以它有用；但别把它当成「已经安全了」。

> ⚠️ **加密必须和 `admin.*` 鉴权一起做，不能单独先上。**
> 只加密而不做访问控制，安全上收益接近于零 —— 号还是谁都能导 ——
> 反而会让人以为已经处理过了。见 §5.2。

---

## 5 · 待修

代码与规格不符的地方（**别在这里写「共几条」** —— 改一条忘一处就对不上）。
每条自带判据：什么算修完。

### 5.1 代码字段名落后于契约

`api/types.ts` 对齐到现行模型之后，代码里这几处还是旧名字 / 旧字段：

| 契约 | 代码现状 |
|---|---|
| `Event.occupiedCount` | 派生字段仍叫 `confirmedCount`（判定逻辑已修，只剩名字） |
| `Event.groupSize` / `qualifyCount` | 没有 |
| `Event.matchFormat` 是结构化对象 | 云函数还没读它；建赛屏的「比赛格式」还是一行只读文字，要改成预设 + 可调 |
| `Tournament.tier` / `clubId` / `coverUrl` | 没有 |
| `Entry.invitedBy` / `referredBy` / `addedByOrganizer` | 没有 |
| `Entry.partnerDeadlineAt` / `paymentDeadlineAt` | **契约里已删，代码还在读写** |
| `PointRecord` / `RankRow` | 整个实体没有 —— `pages/rank/` 还是一张 TBD 占位卡，稿已出（`design/rank/`） |
| `Draw` / `DrawSize` | 整个实体没有 —— `utils/draw.js` 只从 `Match[]` 反推着渲染，签位、轮空、「生成过没有」「拖过没有」全都没处存，所以第 7 屏的拖拽做不了 |
| `Club` / `Membership` | 整个实体没有 —— `admin.*` 鉴权就卡在这：模型里没有可以用来判「他是不是这个俱乐部的管理员」的东西 |
| `User.platformRole` | 没有。建俱乐部要靠它判权限；第一个平台管理员**没有自助入口**，得人工改库引导 |
| `GroupStanding.setsWon` / `setDiff` | 没有 —— 判定顺序里净胜盘排在净胜局之前，缺了排不出名次 |

**契约领先于实现是正常的**，别反过来改 `types.ts` 去迁就旧代码。
这批跟着各自的功能一起做：报名链路重写时补 Entry 的来源字段，
入会审批时补 Membership，小组赛制时补 GroupStanding。

### 5.2 手机号明文存，且 `admin.*` 没有鉴权

**两件事必须一起做**，单独做任何一件都没有意义：

1. `users.phoneEncrypted` 存的是明文，字段名在撒谎（见 §4）
2. `admin.*` 路由**完全没有鉴权** —— `lib/gate.js` 只拦「未注册」，
   任何**已注册**用户都能调 `admin.import` 写数据、`admin.initDb` 建库

现在是 mock 模式所以无所谓；**一旦部署到真环境并且有了真实用户，
这就是「任何人都能导出全部手机号」**。

补的时候一起做的还有：按赛事维度限制管理员范围（只能看自己赛事的报名者）、
全号按需返回、读取记操作日志。规则见 `PRD.md` §9。

> **`adminweb/` 立项之后这条从「待修」升级成「阻塞」**：今天暴露面小是因为
> 根本没有 admin 客户端，后台一上公网静态托管，就变成「公网上一个页面，
> 背后一条谁都能调的路由，返回明文手机号」。**先补鉴权，再写后台页面。**

### 5.3 直播面板：字段定了，代码还没写

`MatchScore` 只存 `sets` + `serveOrder`，**没有「当前这一分」**——
逐分数据明确不上传。所以面板要的那一帧单独推：`Match.liveNow`
（当前分的**显示文本** + 发球方 + 推送时刻），只在 `live` 时有值。

存显示文本而不是原始点数，是因为占先 / 无占先 / 抢七的换算在
`utils/tennis.js` 里。云上再实现一遍，早晚会出现
**直播上显示 40、选手手机上显示 AD**，现场没人说得清哪个对。

面板靠 `Match.code`（`260910-0012`）调出某一场，一个输入框、不用登录，
见 `PRD.md` §6。

**不许反过来要求记分器在线** —— 离线优先是地基。断网时面板停在 `liveNow.at`
那一刻，恢复后自己追上。

**未实现**，且动手前有一个数要拍：**推送频率**。每一分推一次是
60~100 次写 / 场，云数据库按写次数算钱。节流成「每局结束 + 每 N 秒」
能省一个数量级，但节流过头会让人以为面板卡住了。

### 5.4 抽签云函数还没写

规则齐了（`PRD.md` §5）：签位数取最小 2 的幂、随机排、轮空按二进制位反转
顺序占位、抽完可手动拖调。**位反转那段是三行代码**：

```js
// 8 场 → 1 5 3 7 2 6 4 8；16 场 → 1 9 5 13 3 11 7 15 …
const byeOrder = (m) => {
  const b = Math.log2(m);
  return Array.from({ length: m }, (_, i) =>
    parseInt(i.toString(2).padStart(b, '0').split('').reverse().join(''), 2) + 1);
};
```

**未实现。** 小组循环那种赛制还缺分组规则，单败淘汰这条可以先写。

### 5.5 两个详情页的代码用着废弃术语

`tournament/detail` 的界面文案写着「**项目**」「选择**项目**报名」，
`event/detail` 写着「已报名**组合**」「共 N **组**」——「项目」和「组合」
都是明令禁用的词（见 §1 术语表），计数单位也该是「队 / 人」。
稿子已出（`design/tournament/`），照着改即可。**待修。**

> 赛事详情那屏的底部按钮「选择项目报名」应当**整个删掉** —— 每一行比赛
> 就是入口，再放一个通用按钮等于问第二次「选哪个」。

### 5.6 `entry/pay` 这一页应该删掉但还在

规格里写着「原来的 `entry/pay` 删掉：付款用 `wx.requestPayment` 在当前页唤起」
（见 §2），但 `mp/miniprogram/pages/entry/pay/` 还在，`app.json` 里也还
注册着它。**待删** —— 连同 `app.json` 里那一行。

### 5.7 `entry/invite` 还是代付时代的稿

页面文案写着「XX 邀请你组队 · ¥120 **由 XX 支付**」—— 代付这件事早就取消了
（`PRD.md` §4「每人付自己的」），「组队」这个词也退休了（见 §1 术语表）。
整页要按现在的模型重做，不是改一个词的事。**待重做。**

### 5.8 `me/settings` 没有入口

「我的」页上的「设置」行按评审去掉了（见 `design/me/`）。
**账号注销是合规要求，上线前必须给它找个落点** —— 塞进「资料编辑」最省事。

### 5.9 「改性别找组织者」这条路是断的

性别改成**只有后台能改**（`PRD.md` §9，云函数已返回 `GENDER_READONLY`），
但**后台没有任何一屏能改用户资料**（§2）。

于是现在的状态是：用户注册时选错性别 → 自己改不了 → 让他找组织者 →
**组织者也没地方改**。只锁不给出路比不锁更糟，用户会当成 bug 反复试。

两条路，选一条：

| | 代价 |
|---|---|
| 挂在「报名名单」那屏上 | 最省事，但只能改**报过名的人**；没报名就找不到 |
| 加一屏「选手」 | 能覆盖所有人，但要做搜索 —— 后台没有全平台用户列表，也不该有 |

倾向第一条：**会来求助的人几乎一定正在报名**，选错性别就是报不进想报的
那个比赛才发现的。等真出现「没报名也要改」再说。

### 5.10 小组全平不显示并列

判定顺序只有三档（`PRD.md` §5），**三档全平就是并列**，出线由主办方定。
但 `utils/draw.js` 里 `rank` 恒为 `i + 1` —— 并列的两队会被排出一个
1、2 的假名次，表上看不出它们其实没分出来。

组织者照着这个假名次让人出线，输的那队一查是平的，就是一次没必要的争执。
**未实现**：并列要共用名次，出线卡在并列上时要在签表屏上给一个显式的选择。

### 5.11 签表推进：小组循环那半边还没有规则

淘汰赛已经在推了 —— `confirm` → `advance`，按 `Match.sourceMatchAId/BId`
找下游填胜者，守卫在 `test/cloud.test.js` `[7]`。

**小组循环不能照搬**，两个原因：

1. 出线要**整组打完**才算得出来 —— 净胜盘、净胜局要全组比完，一场定不了一个位置
2. 三档全平就是并列（§5.10），系统不往下判 → **必须停下等人**

正确的形状是：整组 `confirmed` → 一次性算 `GroupStanding` → 填 `Draw.slots`；
卡在并列上就停住，等主办方在签表屏上点一下。

> 现在的代码**碰巧不出错**：小组赛的 `Match` 没有下游指着它，`advance`
> 找不到 target 就返回 `null`。那是巧合不是设计，也没有测试守着 ——
> 哪天给小组赛补了 `sourceMatchAId`，它会开始一场一场地乱推。

**动手前要先定一件事**：并列正好卡在出线线上时，界面停在哪、主办方怎么点。

### 5.12 改比分之后下游不会退回去

「改比分」是 P1（`PRD.md` §11），还没做。做的时候绕不开这条：
**改了已确认的比分、胜者变了，`advance` 填进下游的那个位置要跟着改。**

而下游可能已经开打甚至已经出分。所以规则得先定，代码才好写 ——
至少是「下游已开打就不许静默改，得先把下游退回去」。

> 现在 `score` 拦着 `confirmed` 的场次（`BAD_STATE`），所以这个坑暂时踩不到。
> 但那道拦截**正是改比分要拆掉的那道** —— 拆的时候记得连回滚一起补。

### 已修的几条 —— 守卫在哪

修完的不留在上面那张单子里，**但守卫要留个指路**：这些坑都是回归一次就
很难再发现的，所以每条都配了「把它改回去就会红」的断言。

| 修了什么 | 守卫 | 怎么验 |
|---|---|---|
| 名额超发（判据从「只数 `confirmed`」改成正选全数） | `test/entry.test.js` `[3b]` | 退回只数 confirmed → 红 4 条 |
| 候补 `promoted` 状态残留 | 同上 | — |
| 金球判定、默认赛制 `short6_gp` 当初不在 `FORMATS` 里 | `test/tennis.test.js` `[2b]` | 退回占先制或删掉 `noAd: true` → 红 |
| 性别改成用户完全不可改 | `test/cloud.test.js` `[8]` | 退回「有报名才锁」→ 红 2 条，关键那条是**没报过名的干净用户也必须被拒** |
| 报名状态界面叫「正选」 | `test/entry.test.js` `[4]` | 文案改回「已确认」→ 红 |
| 记分 / 确认 / 异议只对**场上的人**开放；异议只在确认之前合法 | `test/cloud.test.js` `[7b]` | 五道守卫任拆一道 → 红 1~6 条 |
