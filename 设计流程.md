# LJTennis 设计流程

> ⚠️ **这是过程日志，不是规范。** 里面有被推翻的决定、作废的方案、
> 以及发现问题的经过。照它写代码会踩到已作废的结论。
>
> **结论看 [`设计定稿0.md`](设计定稿0.md)。** 本文回答的是「为什么这么定」。

> 微信小程序 · 业余网球单双打赛事：报名 → 记分 → 出分

## 交付物索引

| 阶段 | 文件 | 在线预览 |
|---|---|---|
| 阶段 1 信息架构 | `design/ia-v2.html` | https://claude.ai/code/artifact/67c2b34a-2586-4a2c-a3d2-c4ab750efea5 |
| 阶段 2 低保真线框 | `design/wireframes.html` | https://claude.ai/code/artifact/da02c42c-190b-4749-a927-6c66942ebbb5 |
| 阶段 3 设计系统 v0.2 | `design/design-system-v2.html` | https://claude.ai/code/artifact/38437b63-9028-4fa4-81f5-9fd9218ff94d |
| 阶段 4-1 比赛日 | `design/hifi-matchday.html` | https://claude.ai/code/artifact/90297cfd-7e95-4d0e-9fa5-f1f59601e45f |
| 阶段 4-2 报名链路 | `design/hifi-registration.html` | https://claude.ai/code/artifact/426e9c9d-a558-4ad1-b7a0-46da3d1ea33e |
| 阶段 4-3 注册与个人 | `design/hifi-account.html` | https://claude.ai/code/artifact/d3c987a7-da53-4a46-9346-1454fe699a02 |
| 阶段 4-4 积分榜 | `design/hifi-rank.html` | https://claude.ai/code/artifact/6d2dc9bb-2e7e-4a02-b347-0c55a2758950 |
| **开发前置** 数据模型与 API 契约 | `design/api-contract.html` | https://claude.ai/code/artifact/7afe6c5a-dfe2-4de1-b464-186e02d9b1a3 |

代码资产：
- `mp/miniprogram/styles/tokens.wxss` —— 设计令牌 v0.3（已移入小程序，避免两份漂移）
- `design/app.json.draft` —— 21 页 / 3 tab 骨架
- `api/types.ts` —— 实体与状态机的 TypeScript 定义（已过 `tsc --strict`）
- `api/endpoints.md` —— 接口清单、鉴权、幂等、定时任务
- `api/cloudbase-notes.md` —— 云开发对契约的 6 点影响 + 组织者后台结论

## ⚠️ 范围变更（最新）

**积分回到第一版，占第二个 tab。记分收进赛事 tab 首页。**

| | 现状 |
|---|---|
| tabBar | **赛事 / 积分 / 我的**（3 个） |
| 记分入口 | 赛事 tab 首页的**今日比赛区**（置顶），以及签表 / 赛程 |
| 页面 | 规划 **21 页**（主包 16 · 分包 5）。**实际已建 14 页，全在主包，分包从未建立** —— 见下方复盘 |
| 注册字段 | **昵称 · 手机号 · 性别 · 水平**。昵称走 `<input type="nickname">`；手机号**只能微信授权**（已去掉手动输入）；水平是 NTRP 自评 2.0–5.0 |
| 注册触发点 | 两个：点「报名 / 接受邀请」时，或点「我的」tab 时 |
| 双打水平 | **算出来的评分**仍然延后。注册时的**自评水平**在第一版 —— 两件事：自评只做赛事初始分组，不依赖算法 |

### 记分收进赛事 tab 的代价与补偿

比赛日从 **1 tap 变成 2 tap**。补偿是赛事 tab 首页的今日比赛区**永远置顶** ——
只要今天有比赛，打开小程序第一眼就是它，第二下点「进入记分」。

> ⚠️ 如果这一区没做好，实际会退化成 3–4 tap，球场边会直接放弃记分。这是第一版最需要盯的一处。

意外的好处：签表 → 记分器现在是**同 tab 内跳转**，可以直接 `navigateTo` 带参数，
省掉了原来那套跨 tab 的 storage 传参。（本地存储仍必需，断网续记要用。）

### 未注册时三个 tab 的策略

| tab | 未注册 | 理由 |
|---|---|---|
| 赛事 | **不拦**，完整可浏览 | 获客前提，分享落地页要能看 |
| 记分 | **不拦**，复用已有空态 | 没比赛可记，拦了没收益 |
| 我的 | **拦**，显示未注册态页面 | 整屏都是个人数据 |

> ⚠️ 上表第二行写的是「记分」，但 tabBar 早已改成 **赛事 / 积分 / 我的** —— 记分收进了赛事 tab。这一栏应读作「积分」，同样不拦。

「我的」用**未注册态页面**而不是硬跳转 —— tab 页硬跳注册会把人困住退不回去，还会打乱 tabBar 状态。
规则与设置这类不涉及个人数据的入口留在原位。

注册页要携带**来源与回跳目标**：从报名来的完成后继续报名流程，从「我的」来的回到「我的」。
这个参数跨 tab 时要走本地存储 —— `wx.switchTab` 带不了参数。

## 开发进度

小程序工程在 `mp/`，基于官方 quickstart-wx-cloud 模板搭建。详见 `mp/README.md`。

| | 状态 |
|---|---|
| tabBar 三 tab | ✅ 深色模式已全局关闭，统一亮色 |
| 设计令牌接入（零硬编码颜色） | ✅ |
| 计分引擎（纯函数 + 25 项单测） | ✅ |
| 开局设置 + 记分器（轮转/抢七/撤销/离线） | ✅ 假数据可跑 |
| 赛事 tab 今日比赛区 | ✅ 假数据 |
| 签表（轮次分页 + 小组循环 + 全览图） | ✅ |
| 报名链路（详情→项目→组队→支付→我的报名） | ✅ 状态机 + 实时倒计时 |
| 云函数（api + scheduler） | ✅ 64 项单测，已接入 `<你的云环境ID>`，待上传部署 |
| 积分 / 我的主页 | ⬜ 占位 |
| 组织者后台 | 🟢 第一版不做界面 —— 云开发控制台 + 抽签云函数 + CSV 导入 |

主包体积 **0.76 MB / 2 MB（38%）** · 单测 **165 项全过**（`cd mp && npm run verify`）。

### 还没定的

1. **小程序主体资质** —— 微信支付要企业主体 + 商户号，上线前的硬门槛
2. **真名字段** —— 第一版只收昵称，签表显示昵称。真名是纯增量字段，颁奖或出证书时再补
3. **积分规则** —— 界面已按通行假设画好，但下面四项必须有答案才能开发：
   各轮次分值 · 赛事等级系数 · 双打是各得全额还是各半 · 赛季划分方式

---

# 全局阶段图

`设计流程.md` 编到阶段 4 —— **全是设计**。

| 阶段 | 做什么 | 产出 | 状态 |
|---|---|---|---|
| **0 · 定义** | 给谁用、解决什么 | 一句话定义 · 角色 · 核心场景 | ✅ |
| **1 · 信息架构** | tabBar 几项、共几屏、状态机、关键路径 | 页面清单 + 跳转图 | ✅ |
| **2 · 低保真** | 每屏放什么、谁在上面。**不碰颜色字体** | 线框 + 需拍板的结论 | ✅ |
| **3 · 设计系统** | 颜色、字号、间距、圆角、组件 | 令牌表（`tokens.wxss`） | ✅ |
| **4 · 高保真** | 按关键路径分批出稿 | 可交付的屏 | 🟡 第三批阻塞 |
| **5 · 工程 + 假数据** | 搭页面、写纯逻辑、单测。**不连后端** | 能完整点通的小程序 | ⏸ 搁置 |
| **6 · 后端与真链路** | 云函数、数据库、鉴权、端到端打通 | 真数据跑通一条路径 | ⏸ 搁置 |
| **7 · 资质与合规** | 企业主体、隐私指引、商户号、付费能力 | 平台侧全部就绪 | ⏸ 搁置 |
| **8 · 内测与发布** | 体验版、真人试跑、提审 | 线上 | ⏸ 搁置 |

> **当前只做阶段 0–4（设计）。** 5–8 全部搁置：不接后端、不办资质、不发布。
> 期间 `config/env.js` 保持 `useMock: true`，小程序独立可跑，方便走查设计。

## 阶段划分的两条经验

**阶段 5 必须先用假数据跑通。** 界面和后端同时联调，出问题时分不清是哪一头。
本项目的 `config/env.js` 留了 `useMock` 开关，任何时候都能切回假数据定位问题。

**阶段 7 的行政流程要提前并行启动，不能等代码写完。**
企业主体认证、隐私指引审核、商户号申请都以「周」计，而且互相依赖：
没有企业主体就办不了商户号，没过隐私审核 `getPhoneNumber` 就调不通。
这是唯一一个**写代码解决不了、且会真的堵住上线**的阶段。

## 当前的三个阻塞

| 阻塞 | 卡住什么 | 性质 |
|---|---|---|
| 企业主体 + 隐私指引过审 | 手机号授权 → **注册进不去** | 行政，数周 |
| 商户号 | 报名费支付 | 行政，数周 |
| 积分规则四个数字 | 积分页（阶段 4 第三批 + `rank` 页） | 决策，你定 |

前两个只能等，第三个只等你一句话。


---

# 阶段 0–4 复盘

拿设计文档逐条对照已建成的代码。差异分三类：**设计对了但没做完**、
**做了但文档没跟上**、**设计当初就没想到**。

## 一 · 最要紧的一条：假数据是「焊死」的，不是「切换」的

14 个页面里 **10 个直接 `require` 了 mock 模块**，绕过 `utils/cloud.js`。
`config/env.js` 的 `useMock: false` 对它们完全无效 —— 接上真云环境后，
这些页面照样显示假赛事、假搭档、假签表。

| 页面 | 数据来源 |
|---|---|
| `auth/register` · `me` | ✅ 全部走 `cloud.call` |
| `tournament` · `entry/invite` | 🟡 一半走云函数，一半 require mock |
| `draw` · `draw/overview` · `entry/create` · `entry/pay` · `event/detail` · `me/entries` · `score/setup` · `tournament/detail` | ❌ 只有 mock |

阶段 5「先用假数据跑通」的思路是对的，**执行方式错了**：
假数据应该只存在于 `utils/cloud.js` 这一个降级点，页面永远只认 `cloud.call`。
现在假数据散进了 10 个页面，等于把阶段 5 的脚手架浇进了承重墙。

> **这是阶段 6 真正的工作量**，而 `开发进度` 表把这些页标成了 ✅ —— 那个 ✅ 的含义
> 是「界面能跑通」，不是「接上后端了」。表格没区分这两件事，读起来会高估进度。

## 二 · 页面：规划 21，实到 14，分包从未建立

阶段 1 定的 21 页（主包 16 · 分包 5），实际建了 14 页，**全在主包**。

未建的 7 页全部属于「我的」和积分体系：

`player/detail` 选手主页 · `me/records` 我的战绩 · `me/level` 水平详情 ·
`rules/points` 积分规则说明 · `me/profile` 资料编辑 · `me/settings` 设置 ·
`score/confirm` 赛后确认

前五页阻塞于**积分规则未定**（阶段 0 遗留待决项第 1 条，至今没答案）。
`score/confirm` 不阻塞于任何东西，是纯遗漏 —— 而它在阶段 0 的 P2 关键路径上。

分包没建的后果目前不严重（主包 0.76 MB / 2 MB），但补完那 7 页后要复核。

## 三 · 设计当初没想到的

| 发现 | 影响 |
|---|---|
| `user.register` 需要免鉴权 | 分发器把它当受保护接口，**新用户永远注册不进来**。阶段 1 定了「两个分享落地页免登录」，但漏了注册接口本身 |
| `getPhoneNumber` 需企业主体**且按次收费**、`code` 5 分钟一次性 | 阶段 0 只写了「微信支付需企业主体」，没意识到**手机号也是**。收费和 code 时效改变了实现结构（必须授权当场换号） |
| 隐私保护指引要单独过审 | 阶段 0「硬性前提」只列了主体和商户号，漏了这一条。它会直接让注册不可用 |
| `admin.*` 没有任何鉴权 | 设计上办赛权限来自俱乐部管理员身份，但代码里**任何已注册用户都能调 `admin.import`** 往库里塞数据 |
| 系统深色模式会翻卡片内的控件 | 阶段 3 的令牌做了深色适配，但注册页是压在固定底图上的，翻色导致浅卡片嵌深控件。已全局关闭深色 |

## 四 · 做对了、值得保留的

- **Entry 抽象（1~2 人）** 经受住了考验。加自评水平、改支付链路都没动到它
- **阶段 2 拒绝横滚树状图**，改轮次分页 —— 签表是唯一一次做完就再没返工的复杂屏
- **纯函数记分引擎**（`utils/tennis.js`）与界面完全解耦，25 项单测，一次没因为 UI 改动而修过
- **令牌零硬编码** 让「全局关闭深色模式」只花了一次提交

## 五 · 结论：阶段 6 的真实任务清单

按优先级：

1. **把 10 个页面从 mock 迁到 `cloud.call`** —— 这是「接上真链路」的实际含义
2. **补 `score/confirm`** —— P2 关键路径上的遗漏，不阻塞于任何未决项
3. **给 `admin.*` 加管理员白名单** —— 有真实用户之前必须堵上
4. **定积分规则** → 解锁剩余 5 页 + 阶段 4 第三批


---

# 阶段 0 定义 ✅ 已定稿

## 一句话定义

**LJTennis 让业余网球赛事的「报名 → 记分 → 出分」在一个微信小程序里闭环完成。**

## 角色

| 角色 | 在哪 | 能做什么 |
|---|---|---|
| 选手 | 小程序 | 报名、看签表、记分、确认比分、看积分和水平 |
| 俱乐部管理员 | **小程序（现场）+ 网页（赛前）** | 现场：看报名名单、记分、改比分、处理争议<br>赛前：建俱乐部、建赛事、抽签、排赛程、导入 |

**只有两个角色。** 「记分员」和「赛事负责人」都已删除：

- **记分员不是角色，是每场比赛上的一个开关**（见「记分权限模型」）。
  原表里那行自己就写着「不是独立角色」—— 一个声明自己不是角色的东西，不该在角色表里。
- **赛事负责人与组织者在俱乐部模型下是同一个人**：谁能办赛由俱乐部管理员身份决定，
  不再需要第二个头衔。

一个人可以同时是选手和管理员 —— 现实里几乎一定是。不做身份切换，
管理员只是在「我的」页多一块入口。

## 核心场景

三个场景的物理条件完全不同，不能用一套设计标准。

| 场景 | 在哪 | 条件 | 设计原则 |
|---|---|---|---|
| **报名** | 家里、通勤 | 有网、双手、不急 | 信息可以密、表单可以长，重点是把赛事讲清楚 |
| **记分** | 球场边 | 信号差、单手、出汗、90 秒换边 | 极简、大点击区、离线优先、防误触 |
| **看积分/水平** | 赛后任意 | 有网、双手 | 数据可以深，重点是解释「为什么是这个数」 |

## 两个数字：积分 ≠ 水平

这是本产品最容易出错的定义。两者必须在 UI 上物理分开，各配一个说明页。

| | 积分 | 水平 |
|---|---|---|
| 怎么来 | 参赛获得，累加 | 从比分推算 |
| 会不会降 | 不降（赛季清零） | 会升会降 |
| 和参赛频率 | 强相关，打得多分就高 | 无关，打三场也能测准 |
| 用来干嘛 | 排名、种子、参赛资格 | 分组、匹配、预测胜负 |
| 类比 | ATP 积分 | UTR |

**第一版策略**：排行榜**只排积分**（客观、无争议）。水平**算但不上榜**，只出现在个人页，作为成长指标和赛事分组依据。算法跑稳、数据够多之后再考虑上榜。

> 用户一定会问「我赢了为什么积分没涨」「积分涨了为什么水平掉了」。两个问题都合理，只有把数字分开 + 各有说明页才解释得通。

## 参赛项目与赛制

### 建模：赛事 → 项目 → 组合（三层）

一个赛事下设多个**项目（event）**，报名是报项目，不是报赛事。
即使第一版只上双打，也按三层建模 —— 以后加单打不用重构报名流程和签表。

```
赛事 Tournament
 └─ 项目 Event（男双 / 女双 / 混双 …）
     └─ 参赛主体 Entry = 1~2 名选手
         └─ 比赛 Match
```

`Entry` 是关键抽象：单打时是 1 人，双打时是 2 人。签表、积分、记分全部挂在 Entry 上。

### 第一版上线范围

**只上双打**：男双 / 女双 / 混双。混双多一条性别校验规则（每组一男一女），成本很低。
单打暂不上线，但模型已留位。

### 赛制

**只做两种**，覆盖约 90% 的业余赛事：

- **单败淘汰** → 签表是树状图
- **小组循环 + 出线淘汰** → 签表是小组积分表 + 树状图

不做：全循环、瑞士制、康普顿制。

> 签表（draw）是本产品最难的一屏。双打让每格要放两个名字，16 组 = 32 个名字，
> 手机竖屏空间压力翻倍。这是阶段 2 要单独死磕的一屏。

## 组队

**发起人邀请制**：一人发起报名 → 微信转发邀请搭档 → 对方接受 → 组合成立。

报名状态机：

```
发起报名 → 待搭档确认 → 组合成立 → 待支付 → 已确认 / 候补
                ↓ 超时或拒绝
              报名作废
```

要处理的边界情况：
- 搭档没用过小程序 → 占位态，接受邀请时才创建账号
- 搭档赛前退赛 → 换人（钱不动）或整组退赛（走退款）
- 邀请超时 → 自动作废，释放名额

> 转发邀请顺带把没用过小程序的搭档拉进来，是双打场景自带的获客路径。

## 记分权限模型

```
每场指定一名选手记分
   ↓  比赛中，每分 1 tap，全程离线可用
结束自动生成确认卡
   ↓  对方组合任一人在自己手机上 1 tap 确认
比分生效 → 结算积分与水平
   ↓  有争议 → 上报俱乐部管理员裁定
```

未确认的比分在签表和战绩里要有可见的状态标记。

> **补充（重走后）**：管理员随时也能记分，并且可以**对某一场关掉「选手记分」**。
> 一个开关，不是赛事级配置 —— 建赛那天并不知道哪场会直播、哪场会吵起来，
> 等那场要开打了再关才是真实的决策时机。现有记分器与签表不受影响。
>
> 一个连带结果：**管理员记的分不需要选手确认**。管理员本来就有「改比分」的权力，
> 让他记的分再回去等选手批准是矛盾的；真出争议时，让吵架的一方去确认会直接卡死。

### 双打的发球轮转

**记录到「谁在发球」的粒度**，这是双打里最有价值的个人数据（能算出每人的保发率）。

- 开局设定一次发球顺序：A1 → B1 → A2 → B2（2 tap）
- 之后每局自动轮转，用户零操作
- 记分器上的网球黄指示点从「2 选 1」变成「4 选 1」，同时显示当前发球者名字

## 报名与支付

**报名费走微信支付。**

### 硬性前提

微信支付需要**企业主体**的小程序 + 商户号，个人主体开通不了。这是上线前的资质门槛，需要先确认。

### 谁付钱

**发起人付全款。** 搭档接受邀请后，由发起人一人付清。

理由：每多一个付款环节就多掉一批人。两人各付一半时，任一方不付整组卡住，转化会明显下降。
AA 让他们线下自己解决，第一版不做内置收款。

### 状态机

```
发起报名 → 待搭档确认 → 组合成立 → 待支付（发起人，倒计时）→ 已支付 → 确认参赛
              ↓ 超时/拒绝         ↓ 超时
            报名作废           释放名额

                     已确认 → 申请退款 → 退款中 → 已退款
```

### 候补

**候补不预付，转正后 24 小时内支付。**

- 候补门槛低，愿意排队的人多
- 转正时推送订阅消息 + 24h 倒计时
- 超时未付 → 自动顺延给下一位候补（这个自动机制必须做，否则名额会烂在手里）

### 要设计的页面增量

- 待支付状态 + 倒计时
- 「我的报名」里的支付状态与退款进度
- 退款申请入口 + 退款规则说明
- 支付超时自动释放名额（否则名额被占死）

### 退款规则（暂定，可调）

| 情形 | 处理 |
|---|---|
| 赛前 7 天以上取消 | 全额退 |
| 赛前 3–7 天取消 | 退 50% |
| 赛前 3 天内取消 | 不退 |
| 组织者取消赛事 | 全额退 |
| 换搭档 | 不涉及退款 |

## 四条关键路径

**P1 · 报名**
发现赛事 → 赛事详情 → 选项目 → 发起报名 → 邀请搭档 → 搭档接受 → 支付 → 确认参赛 → 赛前提醒（订阅消息）

**P2 · 比赛日**（最高频、条件最苛刻）
打开小程序 → 我的下一场 → 进入记分 → 每分 1 tap → 结束 → 对方确认

**P3 · 赛后**
积分变化 → 水平变化 → 榜单位置 → 下一个赛事推荐

**P4 · 组织者现场**（重走后新增）
打开小程序 → 我的 → 我管理的 → 选赛事 → 看到异常场次 → 处理
物理条件与 P2 相同（球场、信号差、单手），**套用同一套设计标准**。

## 第一版不做

- 自由约球（C2C 撮合）
- 组织者建赛 / 抽签 / 排程（走网页后台）
- 全循环、瑞士制等赛制
- 水平上榜
- 直播比分面板（OBS）—— **不是否决，是延后**：它要逐分实时，与「记分离线优先、
  只存每盘比分」直接冲突。出口是直播场次天然有网，可走在线分支。记分权限已定宽，
  以后加只是多一个消费端，不必改模型
- 视频、社区内容
- 场地预订
- 单打项目（模型已留位，第一版不上线）
- 单人待配对（第一版只支持邀请搭档）
- **算出来的水平**（`systemLevel`）—— 字段留位，算法不做，避免与注册时的自评混淆
- **俱乐部审批** —— 第一版免审核直接加入，出现垃圾俱乐部再加

## 阶段 0 遗留的待决项

进入阶段 2 之前必须有答案，否则页面画不出来。

1. **积分规则本身** —— 赛季怎么划、各轮次多少分、赛事有没有等级系数。双打是两人各得全额还是各得一半。这是产品规则不是 UI，但榜单页和赛事详情页依赖它
2. **冷启动：首批赛事从哪来** —— 决定要不要做「历史成绩导入」
3. **小程序主体资质** —— 微信支付要求企业主体 + 商户号，需确认已具备

> 这三项都不阻塞阶段 1 和阶段 2。积分规则在画榜单页之前给出即可。

## 重走后的修订（第二版）

### 角色：组织者端两个形态都做

第一版原本写「组织者走网页后台，不在设计范围」，实际退化成「你本人在云开发
控制台手动操作」。重走后改为**两端都做**，按操作的物理条件分工：

| 端 | 管什么 | 为什么 |
|---|---|---|
| **小程序** | 看报名名单、改比分、处理争议 | 现场操作，人在球场，只有手机 |
| **网页后台** | 建**俱乐部**、建赛事、抽签、排赛程、批量导入 | 全是赛前操作 |
| **网页后台** | 建赛事、抽签、排赛程、批量导入 | 拖签表和批量操作小屏做不了 |

分界线是**「现场 vs 赛前」**，不是「简单 vs 复杂」。

### 水平：拆成两个字段

阶段 0 原本的「积分 ≠ 水平」现在是**三个数字**，因为注册加了自评。

| 字段 | 来源 | 用户能改 | 第一版 |
|---|---|---|---|
| 积分 | 参赛累加 | ✗ | ✅ 上榜 |
| `selfRatedLevel` 自评 | 注册拖滑杆 | ✓ 随时 | ✅ |
| `systemLevel` 系统 | 比分推算 | ✗ | ⬜ 算法延后，字段先留 |

界面并排显示，标签写全「自评」「系统」。**系统值没算出来时显示「—」，
绝不用自评值顶替** —— 否则用户打输三场发现数字没动，会认为系统坏了。

### 积分规则：赛事级配置，但组织者只选等级

不定全局固定规则，改成建赛时配置。但**组织者选「赛事等级」，不直接填分值**：

| 等级 | 定位 |
|---|---|
| A | 市级以上公开赛 |
| B | 俱乐部公开赛 |
| C | 内部友谊赛 |

各等级的轮次分值由平台固定（具体数字待定，不阻塞画页面）。

> 为什么不让组织者自由填数字：**全局积分榜会失去可比性**。
> 赛事 A 冠军给 1000、赛事 B 给 100，榜首可能只是参加了分值虚高的赛事。
> ATP 榜单成立恰恰因为分值全局统一 —— 1000/500/250 是等级，不是自由输入。

### 硬性前提补两条

阶段 0 原本只列了「微信支付需企业主体 + 商户号」。实做中撞出来的：

1. **手机号快速验证也需企业主体**，且 2023-08-26 起按次收费（约 0.03 元/次）
2. **用户隐私保护指引要单独过审**，未过审 `getPhoneNumber` 直接不可用
   （报 `api scope is not declared`，不是权限错误，容易误判）

### 「第一版不做」补两条

- **算出来的水平**（`systemLevel`）—— 字段留位，算法不做，避免与自评混淆
- ~~组织者任何界面~~ —— **已推翻**，见上方「组织者端两个形态都做」


---

# 阶段 1 信息架构 ✅ 已定稿

完整文档：`design/ia-v2.html`

## tabBar（4 项）

| tab | 职责 | 页数 |
|---|---|---|
| 🏆 赛事 | 发现、报名、组队、支付、签表、赛程 | 8 |
| 🔢 记分 | 状态驱动：进行中 / 今日赛程 / 历史 | 4 |
| 📊 排行 | 只排积分，按项目 / 赛季 / 地区筛选 | 2 |
| 👤 我的 | 水平、战绩、报名记录、设置 | 7 |

命名刻意避开「赛事 / 比赛」这对中文里容易混的词 —— tab 名用动作，不用名词。
不做「消息」tab，通知全部走微信订阅消息。

## 页面总数

21 页：**主包 16**（全部 tab 页 + 报名链路 + 记分链路 + 两个分享落地页 + 签表）、**分包 5**（我的下面的低频页）。

> 签表刻意留主包 —— 球场信号差，放分包会导致现场下载不下来。

## 报名状态机的三个等待态

每一个都必须有超时和自动出口：

| 等待态 | 超时后 |
|---|---|
| ① 待搭档确认 | 报名作废 |
| ② 待支付（发起人） | 释放名额 |
| ③ 候补转正 24h | **自动顺延下一位候补** |

第三条是后端定时任务。不做的话每个空位都会烂在一个不响应的人手里，而业余赛退赛率很高。

## 比赛日路径

打开小程序 →（1 tap）记分 tab → 记分器 → 赛后确认 → 结算积分与水平。
从签表进入记分是跨 tab 跳转，`wx.switchTab` 不支持参数，必须走本地存储 + `onShow` 读取。

## 现在就要定的四个机制

1. **两个分享落地页免登录** —— 赛事详情、组队邀请。登录墙放在「报名 / 接受邀请」这一步，后端需提供无鉴权只读接口
2. **三个等待态都要超时自动流转** —— 后端定时任务 + 前端倒计时
3. **记分全程离线，签表进主包**
4. **通知全部走微信订阅消息** —— 授权在用户主动操作时申请

## 重走后的修订（第二版）

### 顶层多了俱乐部

```
俱乐部 Club                 ← 新增
 └─ 赛事 Tournament
     └─ 项目 Event（男双 / 女双 / 混双）
         └─ 参赛主体 Entry = 1~2 人
             └─ 比赛 Match
```

「谁能办赛」原本悬空 —— 不能人人都建赛事。有了俱乐部它变成：
**你是某俱乐部管理员，所以能以俱乐部名义办赛。** 权限有了自然边界，
平台不必维护全局组织者白名单。

顺带三个好处：赛事有品牌（「XX 俱乐部 · 春季双打赛」比光秃秃的赛事名可信）、
俱乐部主页是**长期有效的分享落地页**（单场赛事过期就废）、
以及贴合现实 —— 业余赛几乎全是俱乐部或球馆办的，没有「个人办赛」这种形态。

### 选手与俱乐部：可加入，但不影响报名

| | 规则 |
|---|---|
| 报名 | **完全开放** —— 谁的赛都能报，不要求先入会 |
| 加入 | 可选。「我的」页多一块「我的俱乐部」，俱乐部主页能看到成员 |
| 审批 | **第一版免审批直接加入**，管理员可移除成员（我的判断，可推翻） |

封闭制（必须入会才能报名）会挡住分享拉新 —— 朋友转发的报名链接，
对方要先申请入会，转化会断在那一步。

### tabBar：不给组织者开 tab

`tabBar` 是 `app.json` 静态配置，所有人看到的一样。按身份显示不同 tab
只能上自定义 tabBar —— 一整套自绘组件，还要自己处理选中态、红点、跨页同步。
而组织者是极少数人，为 1% 的用户让 99% 的人多看一个 tab 不划算。

**保持三项：赛事 / 积分 / 我的。组织者入口在「我的」页里，只对管理员显示。**

### 积分榜：全局为主，可按俱乐部筛选

默认全局（「全城第几」是更强的动机），筛选器里可只看某俱乐部。
可比性由阶段 0 定的 A/B/C 等级保证 —— 组织者选等级，分值由平台固定。

筛选维度从「项目 / 赛季 / 地区」改为 **「项目 / 赛季 / 俱乐部」**，地区去掉。

### 页面总账：26 页（阶段 2 加了 `org/tournament`，又把 `club/create` 移出小程序）

| | 页面 |
|---|---|
| **主包 17** | 三个 tab（`tournament` `rank` `me`）· `auth/register` · `tournament/detail` · `event/detail` · `club/detail` · `entry/create` · `entry/invite` · `entry/pay` · `draw` · `draw/overview` · `score/setup` · `score/live` · `score/confirm` · `org/match` · `me/entries` |
| **分包 9** | `me/records` · `me/level` · `me/profile` · `me/settings` · `player/detail` · `rules/points` · `org/index` · `org/tournament` · `org/entries` |

进主包的三条理由，只要占一条就进：
**① 分享落地页**（`tournament/detail` `event/detail` `club/detail`）、
**② 球场里要用**（`draw` `score/*` `org/match` —— 信号差，分包下不下来）、
**③ 高频**（三个 tab、`me/entries`）。

> `org/match` 进主包的理由和记分器完全一样：**组织者也站在球场边**。
> 它是 P4 路径上唯一的重操作，条件和 P2 一样苛刻。

### 关键路径多了一条

**P4 · 组织者现场**
打开小程序 → 我的 → 我管理的 → 选赛事 → 看到异常场次（有争议 / 比分未确认）→ 处理

物理条件与 P2 相同（球场、信号差、单手），**必须套用同一套设计标准**：
大点击区、极简、可离线查看。这条路径以前完全不存在，阶段 2 要为它画线框。

### 组织者的网页后台：第一版不画

建赛、抽签、排程、CSV 导入走网页，但**现在先不设计**。
它的形态高度依赖你实际怎么办赛，而你还没办过一场 ——
等小程序端跑通一次真实赛事再画，那时不会白画。

# 阶段 2 低保真线框 ✅ 已出稿，待确认

完整线框：`design/wireframes.html`（11 屏，灰白稿）

## 三个需要拍板的结论

### 1 · 签表用「轮次分页」作主视图

对比了三个方案：

| 方案 | 结果 |
|---|---|
| A 横滚树状图 | 名字压到 9.5px 才排得下，双轴滚动，**不采用** |
| B 轮次分页 | 每场占满整行，两个名字正常字号排得下，单轴滚动，**采用** |
| C 小组循环表 | 不是 A/B 的竞品，是另一种赛制的形态，小组赛阶段用 |

完整 bracket 保留为**「全览图」二级入口**（全屏、可缩放、canvas），不参与日常路径。
理由：真实场景 90% 是「我下一场打谁」和「XX 那场打完没」，不是欣赏整张签表。

### 2 · 记分器改成上下堆叠

双打每边要放两个名字，左右分栏宽度不够。上下堆叠还贴合球场物理方位：对手在上，我方在下。

**好消息：双打没有让记分器变复杂。** 得分按「队」记不按人，点击区仍然只有两块（各约 170pt 高）。
四个人只影响「谁在发球」的显示 —— 发球标记挂在具体的人身上，每局自动轮转。

> ⚠️ 设计系统 v0.1 里的记分器组件是左右并排的，阶段 3 要重画。

### 3 · 积分榜排「人」不排「组合」

双打积分两人各得全额，搭档会换，排组合没有连续性。
代价是榜单上看不到「谁和谁是一对」，需要在选手主页补「常搭档」入口。

## 其他已确认的细节

- 可信度不足时，水平**显示为区间**（6.8–7.6）而不是确切值 —— 阶段 0 的规则第一次落地
- 积分榜要显示**参赛场次**，否则「打得多分就高」会显得榜单不公平
- 满员项目显示**候补人数**，按钮变「加入候补」，不灰掉
- 组队邀请页要写死「谁付钱」—— 被邀请方最关心这个
- 小组出线规则直接写在积分表下面，不藏进规则页

## 还没画的屏（结构风险低，阶段 4 直接出高保真）

赛事列表、支付确认、我的报名、赛后确认、选手主页、我的战绩、水平详情、规则说明、资料编辑、设置

## 重走后的修订（第二版）

原来的 11 屏线框里 **P4 组织者路径一屏都没有**。本轮新增五屏、改画两屏。

| | 屏 |
|---|---|
| 全新 | 组织者首页 · 单场赛事管理 · 报名名单 · 改比分 · 俱乐部主页 · 建俱乐部 |
| 改画 | 积分榜（筛选维度变了）· 我的（多两块） |

### 1 · 组织者首页用「赛事列表 + 待办角标」

主视图是**俱乐部 → 赛事**的两级列表，每个赛事行右侧带待办数角标。

```
我管理的

深圳网球会
├ 春季双打赛 · 进行中        ③
└ 冬季团体赛 · 已结束

城北网球会
└ 元旦友谊赛 · 报名中        ①
```

> 曾考虑把首页直接做成**跨赛事的待办清单**（问题优先而非结构优先）。
> 否决理由：组织者可能管多个赛事，脱离赛事上下文的「场次 12 有争议」
> 反而要回想那是哪场比赛。**列表给出结构，角标保住速度** —— 哪场需要我，
> 打开就看见，不必逐个点进去翻。

点进赛事 → **单场管理页**（新增 `org/tournament`），承载待办清单 +
报名名单入口 + 场次入口。页面总账 26 → 27，随后 `club/create` 移到网页端，回到 **26**（分包 9）。

> **创建俱乐部走网页**：它是赛前操作，落在「现场 vs 赛前」分界线的网页一侧。
> 连带结论：**组织者身份从网页端开始**，小程序只承接后续 ——
> 所以组织者首页一个俱乐部都没有时是空态加一句指路，不给按钮（点了也做不了）。

待办类型按紧急度排序：
比分有争议 › 已提交超时未确认 › 报名超时未付款 › 候补待转正 › 场次已过未开始

### 2 · 改比分只有三行

模型不存逐分数据（`MatchScore` 只有 `sets` + `serveOrder`），
所以改比分 = 编辑最多 3 盘的局分，每盘两个数字。

```
张伟 / 李强   vs   王芳 / 赵敏
第一盘  [6] - [4]
第二盘  [3] - [6]
第三盘  [7] - [6]   抢七 [7]-[5]
                    保存
```

> 这是「v1 不存逐分数据」的**第二次回报**（第一次是记分引擎能写成纯函数）。
> 当初为省事做的取舍，让一个本以为很重的屏变轻了。

### 3 ·「我的」页分三层，不堆列表

现在有 6 类内容，堆长列表会让组织者入口沉到底。

| 层 | 放什么 | 判据 |
|---|---|---|
| 顶 | 身份卡：昵称 · 积分 · 自评/系统水平 | 静态展示 |
| **中** | 我的报名（状态角标）· 我管理的（待办数） | **带数字或状态的** |
| 底 | 战绩 · 我的俱乐部 · 设置 | 纯跳转入口 |

### 4 · 榜单「按俱乐部筛选」筛的是赛事，不是人

选手可加入 0 个或多个俱乐部，榜单行显示哪一个？**都不显示。**

「按俱乐部筛选」= 只统计**在该俱乐部办的赛事里**拿到的积分。
选手的俱乐部归属是社区层，与积分无关。

榜单行仍然只有：名次 · 昵称 · 积分 · 参赛场次。

**筛选器布局**：项目（男双/女双/混双）用一行 chips 常驻（高频切换）；
赛季与俱乐部收进「筛选」抽屉（低频）。

### 复核第一版的三个结论

| 结论 | 现在 |
|---|---|
| 签表用轮次分页 | ✅ 不受影响 |
| 记分器上下堆叠 | ✅ 不受影响 |
| 积分榜排人不排组合 | ✅ 成立，但需补上面第 4 条消歧 |

# 阶段 3 设计系统 ✅ v0.2 已定稿

完整规范：`design/design-system-v2.html` · 令牌：`styles/tokens.wxss`
（v0.1 是约球版，已作废）

## 颜色

主色：**硬地场蓝 #2E4FD8**
语义色：红绿（赢/输）+ 橙（待处理）。网球黄只保留一个用途：**发球方指示点**（双打里指四人中的当前发球者）

头像色板刻意是一组**冷色**（`--c-av-1..5`），不含绿/红/橙 —— 一个绿色头像会被误读成「这个人赢了」。

报名状态 → 颜色是**固定映射**，不可临时改：
待搭档确认 / 待支付 / 候补 = 橙，已确认 = 绿，已退款/作废 = 灰。

## 从 v0.1 的变更

| 项 | 变更 |
|---|---|
| 记分器 | 左右并排 → **上下堆叠**，每块 126pt 高，容纳两个名字 |
| `--t-score` | 120rpx → **96rpx**，给两行名字让位 |
| 发球指示 | 2 选 1 → **4 选 1**，A1→B1→A2→B2 每局轮转；抢七每两分换 |
| 新增组件 | 签表场次卡、小组积分表、榜单行、名额进度、倒计时、状态标签、积分/水平双数值卡 |
| 新增令牌 | `--t-set`、`--c-void`、`--c-av-1..5` |

## 七条排版规则

1. **双打组合名**：宽容器一行 `A / B`，窄容器两行堆叠，**绝不只截断其中一个**
2. **抢七比分用上标** `7⁷ 6⁵`，不用 `7-6 (7-5)`
3. **发球指示是网球黄的唯一用途**
4. **水平可信度 < 60% 显示为区间** `6.8–7.6`，用 en dash 不用连字符
5. **满员不是错误状态** —— 用橙不用红，按钮变「加入候补」不灰掉
6. **头像色不能用语义色**
7. **倒计时必须写明超时后果** —— 「23:41」无意义，「超时释放名额 23:41」才有

## 记分器已验证的逻辑

规范页里的记分器是可运行原型，已验证：
平分/占先/扳平 → 6-6 自动进抢七 → 抢七分数改原始计数 → 7-6 结束并记为 `7⁷ 6⁵` → 撤销回退。
发球轮转：局间 A1→B1→A2→B2；抢七内第 1 分后换，之后每 2 分换 —— 符合 ITF 规则。

## 重走后的修订（第二版 · 令牌 v0.4）

### 表头三处已作废

| 原文 | 现状 |
|---|---|
| `v0.3 — 第一版不做积分` | 积分早已回到第一版 |
| `深色模式：darkmode: true` | **全站不做深色**，`npm run check` 守着 |
| `榜单行、双数值卡不再使用` | 重新启用 |

第一条最危险 —— 照着写会以为积分不做。

### 四个新元素的归宿

**待办角标**：语义与 `--c-live` 一致（有事待处理），**复用不新增颜色**，
只补一个 `--c-on-live`。

**赛事等级 A/B/C**：用**主色三档强度**，不新开色相。

| 等级 | 样式 |
|---|---|
| A | 实心 `--c-brand` + 白字 |
| B | `--c-brand-weak` 底 + `--c-brand` 字 |
| C | 透明 + `--c-line` 描边 + `--c-ink-3` 字 |

> 两条理由。**色轮上没有空位了** —— 绿=赢+微信、红=输/危险、橙=进行中、
> 黄=发球、紫青=头像。更要紧的是 **A/B/C 是有序不是并列**：三个色相只能
> 表达「三种」，表达不了「三档」。同色三档一眼就是排名。
> 风险是实心蓝标签像按钮，靠形态区分（pill 圆角 + 20rpx 字 + 不可点）。

**空值「—」**：`--c-ink-3`，字号与真值一致。两条规矩 ——
**必须与加载态形态不同**（空值是实心字符，加载是骨架条）；
**字号不能缩**，否则真值出来那一刻整块会跳。

**控件高度进令牌**：`--h-control: 88rpx`（输入框/分段/副按钮）、
`--h-cta: 96rpx`（主按钮）。原来这个数字散在页面样式里。

### 第八条排版规则

> **球场边使用的所有可点元素不低于 88rpx。**

约束 P2 与 P4 两条路径上的全部界面。阶段 2 画改比分时数字框画成了 38×32，
就是漏了这条 —— 球场边单手点会点错。

### 顺带修的一个守卫 bug

`check-theme.js` 是纯文本匹配，令牌表注释里写「我们为什么不用
prefers-color-scheme」会被判成违规。改成先剥块注释再查 ——
守卫要看真实的 CSS，不是文字。两个方向都验过。

# 阶段 4 高保真

## 第一批 · 比赛日全链路 ✅ 已出稿

`design/hifi-matchday.html` — 13 屏，全部用设计系统 v0.2 令牌绘制。

覆盖：记分 tab 三态 → 开局设置 → 记分器 → 提交/确认/异议 → 签表（淘汰赛 + 小组赛 + 骨架屏）→ 极端数据。
记分器与签表可直接操作。

### 结论

**设计系统 v0.2 够用，没有新增组件。** 唯一微调：记分器底部补一个「结束比赛」出口，阶段 3 的组件漏了。

**量出了双打名字换行的确切触发点。** 375pt 设备上场次卡内可用宽 321pt，三盘比分列占 67pt，
名字列可用 **244pt**。实测一行宽度：

| 名字对 | 一行宽度 |
|---|---|
| 张伟 / 李强 | 77pt |
| 欧阳建国 / 司马雨欣 | 137pt |
| Wang Xiaoming / Zhang Weiguo | 227pt |
| Christopher Andersen / Aleksandr Volkov | 288pt |

**规则写死：一行超过 244pt 就切两行堆叠。** 中文姓名永远触发不了，只有长英文名会 ——
所以这条规则平时看不出作用，一旦有外籍选手就必须有。

> ⚠️ 若以后支持五盘制，比分列涨到约 110pt，名字列只剩 200pt，中等英文名也会开始堆叠，需复核布局。

### 已覆盖的状态

空状态（新用户记分 tab）、加载态（签表骨架屏）、错误态（比分异议）、
离线态（记分器未同步）、极端数据（超长中英文名、三盘抢七、零数据）

## 第二批 · 报名链路 ✅ 已出稿

`design/hifi-registration.html` — 12 屏。赛事列表筛选可交互，五个倒计时真的在走。

覆盖：赛事列表（含加载/空态）→ 赛事详情（免登录）→ 项目详情（未满/已满）→ 发起报名选搭档 →
组队邀请（免登录）→ 支付（含失败态）→ 我的报名五状态 → 候补转正 → 极端数据。

### 三个新发现

**1 · 小程序拿不到微信好友列表，这改变了组队的形态。**
「选搭档」不可能做成通讯录式选择，只能三条路并存：常搭档列表（我们自己的数据）、
搜索昵称/手机号、直接转发邀请。

> 好的一面：**「常搭档」从锦上添花变成必需品** —— 它是唯一不需要用户输入就能选到人的入口，
> 而且天然驱动双打复购。建议第一版就要有，且排在最上面。

**2 · 免费赛事要能整段跳过支付。**
报名费为 0 时，组合成立后直接进「已确认」，不经过待支付。
否则第一场社区友谊赛会卡在一个支付 ¥0 的页面上。

> ⚠️ 这条阶段 0 和阶段 1 都漏了，是画极端数据时发现的。
> **已补进报名状态机并实现**（`utils/entry.js` 的免费赛事捷径，42 项单测覆盖）。

**3 · 赛事名两行封顶的容量是 40 个中文字。**
321pt 可用宽、16px 中文一行 20 字。实测 39 字的超长赛事名刚好不会被截断，
省略号极少触发。但列表卡要**固定按两行预留高度**，否则滚动时卡片高度参差。

### 新增的一个颜色约束

`--c-wx` 微信绿 `#07C160` 是**唯一允许出现的品牌外颜色**，只用于微信支付按钮，
别处禁用 —— 和网球黄一样的约束逻辑。需要补进设计系统规范页。

## 第三批 · 榜单与我的 · 阻塞中

积分榜、选手主页、我的、水平详情、规则说明

> ⚠️ **阻塞项：积分规则**。赛季怎么划、各轮次多少分、双打两人各得全额还是各得一半。
> 榜单一行显示什么完全取决于它，没有规则画不了。
